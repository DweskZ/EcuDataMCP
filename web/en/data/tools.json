[
  {
    "category": "Unified entry points",
    "tools": [
      {
        "name": "list_capabilities",
        "description": "Summarizes sources, tools, prompts, and server limits.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Describe what this Ecuador MCP can do: sources, key tools, prompts and limits. Call this first when you are unsure which tool to use."
      },
      {
        "name": "search_ecuador",
        "description": "Searches datasets, orgs, procedures, regulations, contracts, and risk events all at once.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Search terms in Spanish (e.g. \"salud\", \"RUC\", \"medicinas\")"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "5",
            "description": "Max results per section (default 5, max 10)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Unified search across Ecuador open data, trámites, regulations, contracts and risk events. Use this as the first step for broad questions. Then drill down with get_dataset_info, query_resource_data, get_tramite_info, get_regulacion_info, get_contrato_info or search_eventos_riesgo."
      },
      {
        "name": "lookup_ubicacion",
        "description": "Provinces, cantons, and parishes (INEC code, region, population).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Name or code (e.g. \"Pichincha\", \"Cuenca\", \"Tumbaco\", \"170150\")"
          },
          {
            "name": "region",
            "type": "str",
            "default": "''",
            "description": "Optional natural region for provinces/cantons"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Optional province filter"
          },
          {
            "name": "canton",
            "type": "str",
            "default": "''",
            "description": "Optional canton filter (useful for parroquias)"
          },
          {
            "name": "nivel",
            "type": "str",
            "default": "'auto'",
            "description": "auto | provincia | canton | parroquia"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Look up Ecuador DPA geography: provinces, cantons and parroquias (INEC codes). - nivel='provincia' | 'canton' | 'parroquia' | 'auto' - For parroquias, filter with canton= and/or provincia= (recommended)"
      }
    ]
  },
  {
    "category": "Open Data",
    "tools": [
      {
        "name": "search_datasets",
        "description": "Search datasets by keyword. Supports filtering by category.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Search keywords (e.g. \"empleo\", \"salud\", \"presupuesto\", \"SRI recaudación\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1)"
          },
          {
            "name": "page_size",
            "type": "int",
            "default": "20",
            "description": "Results per page (default: 20, max: 100)"
          },
          {
            "name": "category",
            "type": "str",
            "default": "''",
            "description": "Optional category filter (e.g. \"sal\" for Salud, \"edu\" for Educación). Use list_categories to see all available categories."
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search for datasets on Ecuador's open data portal (www.datosabiertos.gob.ec). This is the starting point for exploring government data from 98+ public institutions. Use short, specific queries in Spanish for best results. Typical workflow: search_datasets → list_dataset_resources → preview_resource_data"
      },
      {
        "name": "list_recent_datasets",
        "description": "The most recently updated datasets on the portal.",
        "params": [
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default 1)"
          },
          {
            "name": "page_size",
            "type": "int",
            "default": "15",
            "description": "Results per page (default 15, max 50)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List the most recently updated datasets on Ecuador's open data portal. Useful to discover new or freshly refreshed government data without a specific keyword. Sorted by metadata_modified descending."
      },
      {
        "name": "get_dataset_info",
        "description": "Detailed dataset metadata: title, description, organization, tags, license, dates.",
        "params": [
          {
            "name": "dataset_id",
            "type": "str",
            "default": null,
            "description": "The dataset ID or slug (e.g. \"registro-estadistico-de-recursos-y-actividades-de-salud-2019\")"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed metadata about a specific dataset from Ecuador's open data portal. Returns title, description, organization, tags, resource count, creation/update dates, license, update frequency, the publisher's source URL (where the entity keeps the authoritative data, if any), and any custom metadata fields the publisher added."
      },
      {
        "name": "list_dataset_resources",
        "description": "Lists all files (resources) in a dataset with format, size, URL, and creation/modification dates.",
        "params": [
          {
            "name": "dataset_id",
            "type": "str",
            "default": null,
            "description": "The dataset ID or slug"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List all resources (files) in a dataset with their metadata. Returns resource ID, title, format, size, download URL, and creation/ last-modified dates for each file. The dates let you tell whether a dataset's files are periodic snapshots (compare last_modified across resources to find the most recent one) before reading any of them. Also flags groups of resources whose names look like a periodic series (e.g. weekly files) — that's only a hint that they might need combining or picking the latest one; it does not tell you which, since that depends on the dataset (some periodic files replace previous ones, others complement them). Next step: use preview_resource_data on a CSV resource to see its contents, or use get_resource_info for detailed metadata."
      },
      {
        "name": "get_resource_info",
        "description": "Detailed information about a specific file.",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "The resource UUID"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a specific resource (file) from Ecuador's open data portal. Returns format, size, MIME type, download URL, and dataset association. For CSV files, you can then use preview_resource_data to see the actual data."
      },
      {
        "name": "preview_resource_data",
        "description": "Preview of CSV/TSV, JSON/GeoJSON, or Excel (XLS/XLSX) as a table (max 5 MB).",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "The resource UUID (get it from list_dataset_resources)"
          },
          {
            "name": "rows",
            "type": "int",
            "default": "20",
            "description": "Number of data rows to preview (default: 20, max: 100)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Download and preview a resource from Ecuador's open data portal. Supports CSV/TSV, JSON/GeoJSON and Excel (XLSX). Returns the first N rows as a formatted table so the model can inspect data without a local download. Geometry/WKT columns are dropped from the table (they can be tens of KB per cell); CSV columns in European decimal notation (7.760,2) are normalized to standard notation (7760.2). Max download size: 5 MB. For large tabular DataStore resources prefer query_resource_data."
      },
      {
        "name": "download_resource",
        "description": "Downloads the raw file of a resource as base64 (max 5 MB), for formats that can't be previewed as a table (.rar, .tar.gz, etc.).",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "The resource UUID (get it from list_dataset_resources)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Download a resource's raw bytes from Ecuador's open data portal, base64-encoded. Use this for formats preview_resource_data can't parse into a table (.rar, .tar.gz, legacy .xls, or anything unrecognized) — it fetches the file as-is instead of trying to read it. Max size: 5 MB (same cap as preview_resource_data). Larger files come back with an error and the direct URL instead, since they'd be too big to embed in a response. Call with format=\"json\" to get the actual file content — the default format=\"text\" only confirms the download and tells you to retry with json; it never includes content_base64."
      },
      {
        "name": "query_resource_data",
        "description": "Tabular query via the CKAN DataStore (filters, text, pagination) without downloading the file.",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "Resource UUID (from list_dataset_resources)"
          },
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Optional full-text search across datastore fields"
          },
          {
            "name": "filters_json",
            "type": "str",
            "default": "''",
            "description": "Optional JSON object of exact field filters, e.g. '{\"provincia\":\"Pichincha\"}'"
          },
          {
            "name": "rows",
            "type": "int",
            "default": "20",
            "description": "Number of records to return (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset (default 0)"
          },
          {
            "name": "sort",
            "type": "str",
            "default": "''",
            "description": "Optional sort expression, e.g. \"anio desc\""
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Query a tabular resource via CKAN DataStore without downloading the full file. Prefer this over preview_resource_data when the resource is in the DataStore (most CSV resources on the portal). Supports full-text query, JSON filters, sorting and pagination."
      },
      {
        "name": "detect_series_pattern",
        "description": "For datasets that publish one file per period: tells whether each new file replaces the previous ones (cumulative) or complements them (incremental).",
        "params": [
          {
            "name": "dataset_id",
            "type": "str",
            "default": null,
            "description": "The dataset ID or slug"
          },
          {
            "name": "resource_id_new",
            "type": "str | None",
            "default": "None",
            "description": "Optional -- newer resource ID to compare (auto-detected from the dataset's periodic-name group if omitted)"
          },
          {
            "name": "resource_id_old",
            "type": "str | None",
            "default": "None",
            "description": "Optional -- older resource ID to compare (auto-detected if omitted). Both resource_id_new/resource_id_old must be given together, or neither."
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "When a dataset publishes one file per period (weekly/monthly), it's not always clear whether each new file replaces the previous ones (cumulative -- only the newest file matters) or complements them (incremental -- every file must be combined). Downloads the two most recent resources from the periodic-name group list_dataset_resources already detects, finds a date/period-like column by header name (fecha, mes, año, periodo, semana, ...), and compares which period values appear in both files. High overlap → cumulative; near-zero overlap → incremental; if no period column is found or the overlap is ambiguous, returns indeterminado rather than guessing. A heuristic over a sample of up to 500 rows per file -- confirm with preview_resource_data if the result is close to the threshold."
      },
      {
        "name": "search_sri_datasets",
        "description": "Search across ~130 SRI files published outside the CKAN portal: RUC registry by province, tax collection, sales/purchases, vehicles, CEL, data dictionaries.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the file's label or URL (accent-insensitive), e.g. \"recaudación\", \"vehiculos\", \"Pichincha\". Empty returns all files."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max results (default 30, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search the SRI's open datasets page (sri.gob.ec/datasets) for direct file links. Covers ~130 CSV/XLSX/ZIP files not reachable through search_datasets (they live on the SRI's own site, not the CKAN DataStore): RUC catastro by province, recaudación by year, ventas/compras, vehículos nuevos, comprobantes electrónicos (CEL), inscripciones/cierres, plus variable dictionaries (*_DD.xlsx). Returns direct URLs, not the file contents — download them yourself or via download_resource."
      }
    ]
  },
  {
    "category": "Government procedures",
    "tools": [
      {
        "name": "search_tramites",
        "description": "Search Ecuadorian government procedures (ID card, passport, RUC, driver's license, etc.).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Keywords to filter results (e.g. \"RUC\", \"inscripción RUC persona natural\"). Each word must appear in the trámite name or description."
          },
          {
            "name": "institution_id",
            "type": "str",
            "default": "''",
            "description": "Institution ID (strongly recommended for relevant results)"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1). Ignored when query is provided (all pages are searched automatically)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search for government procedures (trámites) on Ecuador's official portal gob.ec. Best approach: provide an institution_id to get that institution's trámites. Add a query to filter results by keyword (filters by name and description). When query is provided with institution_id, ALL pages of that institution's trámites are fetched and filtered, so you always get the most relevant results. Common institution IDs: - \"8\" = SRI (Servicio de Rentas Internas) — impuestos, RUC, facturación - \"23\" = Registro Civil — cédula, partidas de nacimiento - \"62\" = ANT — licencias de conducir, matriculación vehicular - \"16\" = Ministerio de Relaciones Exteriores — pasaportes, apostilla - \"5\" = IESS — seguro social, pensiones, fondos de reserva Use list_instituciones to find more institution IDs."
      },
      {
        "name": "get_tramite_info",
        "description": "Full detail: requirements, procedure, cost, estimated time.",
        "params": [
          {
            "name": "tramite_id",
            "type": "str",
            "default": null,
            "description": "The procedure ID (e.g. \"18009\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a specific government procedure (trámite). Returns the procedure name, description, requirements, beneficiaries, cost, and the institution responsible. Get the tramite_id from search_tramites."
      },
      {
        "name": "list_instituciones",
        "description": "List Ecuador's public institutions.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Optional search term (e.g. \"SRI\", \"salud\", \"rentas\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1, only used without query)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List or search public institutions registered on Ecuador's gob.ec portal. If a query is provided, searches across all institutions by name or acronym. Without a query, returns a paginated list. Common institutions: SRI (ID: 8), Registro Civil (ID: 23), ANT (ID: 62), Cancillería (ID: 16), IESS (ID: 5), Ministerio de Salud, INEC, BCE."
      },
      {
        "name": "get_institucion_info",
        "description": "Detail of an institution (sector, website, description).",
        "params": [
          {
            "name": "institucion_id",
            "type": "str",
            "default": null,
            "description": "Institution ID (e.g. \"8\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a public institution registered on gob.ec. Returns name, acronym, sector, description, website and portal URL. Get the institucion_id from list_instituciones. Common IDs: SRI=8, IESS=5, Registro Civil=23, ANT=62, Cancillería=16."
      }
    ]
  },
  {
    "category": "ANDA (INEC)",
    "tools": [
      {
        "name": "search_anda",
        "description": "Search INEC's ANDA catalog of surveys and censuses (NADA/IHSN). Indicates whether each survey has downloadable microdata.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Search keywords (e.g. \"empleo\", \"REEM\", \"censo agropecuario\")"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "10",
            "description": "Max results (default: 10, max: 50)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search INEC's ANDA catalog (anda.inec.gob.ec) of surveys and censuses. ANDA runs on NADA (World Bank/IHSN), separate from datosabiertos.gob.ec. It catalogs 437+ INEC surveys/censuses with metadata (title, year, authoring entity). Not every entry has downloadable microdata — many are aggregate-only publications (e.g. price indices); each result says so. Follow up with get_anda_survey_info(idno) for full metadata on one survey."
      },
      {
        "name": "get_anda_survey_info",
        "description": "Full metadata for an ANDA survey: summary, variables, confidentiality, and contact.",
        "params": [
          {
            "name": "idno",
            "type": "str",
            "default": null,
            "description": "Survey identifier from search_anda (the \"idno\" field)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get full metadata for one ANDA (INEC) survey/census by its idno. Get the idno from search_anda results — it's the string identifier (e.g. \"IDD-ECU-INEC-CGTPE-DECON-ENESEM-2023-v1.3\"), not the numeric id. Returns scope/abstract, variable count, whether microdata is actually downloadable, confidentiality terms, and a contact email for requesting restricted data when it isn't."
      },
      {
        "name": "download_anda_microdata",
        "description": "Direct download links for an ANDA survey's microdata files.",
        "params": [
          {
            "name": "idno",
            "type": "str",
            "default": null,
            "description": "Survey identifier from search_anda (the \"idno\" field)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get direct download links for an ANDA survey's microdata files. Automates ANDA's one-click usage-terms step (research/statistical use only, no re-identifying respondents, cite the source — see get_anda_survey_info for the full text) to reveal the file list. Check get_anda_survey_info first: aggregate-only surveys (microdatos_disponibles false) have no files here. Returns direct URLs, not the file contents — the files are typically multi-MB ZIPs (SPSS .sav inside), too large to embed in a tool result. Download them with the returned URL."
      }
    ]
  },
  {
    "category": "Macroeconomics (BCE)",
    "tools": [
      {
        "name": "search_indicadores_bce",
        "description": "Search the Central Bank of Ecuador's statistical catalog (monetary/financial, public finance, external sector, real sector).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the group's description, its section/subsection, and its individual series labels (accent-insensitive)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search the Banco Central del Ecuador (BCE) statistical catalog. Covers monetary/financial statistics, public finances, external sector (foreign trade), and real sector (GDP, employment/ unemployment, consumer confidence, oil production, cement production, and more) — each result is an indicator group (id_grupo) whose time series data you fetch with get_indicador_bce. Some groups hold a single series, others dozens under a broader title (e.g. \"desempleo\" is a series inside the \"mercado laboral\" group, not a group of its own — matched here too, not just group titles). Does NOT cover Ecuador's own inflation (CPI) or poverty statistics — those are INEC's domain, try search_anda instead. First call after the cache expires (24h) takes ~10-15s: matching against series names, not just group titles, means fetching every group's metadata (~78 groups) once to build the search index. Cached after that."
      },
      {
        "name": "get_indicador_bce",
        "description": "Time series for an indicator by id_grupo: configurable period, frequency, and unit (defaults set per group).",
        "params": [
          {
            "name": "id_grupo",
            "type": "int",
            "default": null,
            "description": "The group id from search_indicadores_bce."
          },
          {
            "name": "desde",
            "type": "str",
            "default": "''",
            "description": "Start period as YYYY-MM. Defaults to the earliest available period for the chosen frequency."
          },
          {
            "name": "hasta",
            "type": "str",
            "default": "''",
            "description": "End period as YYYY-MM. Defaults to the latest available period for the chosen frequency."
          },
          {
            "name": "frecuencia",
            "type": "str",
            "default": "''",
            "description": "Semanal | Mensual | Trimestral | Anual. Defaults to the group's first available frequency."
          },
          {
            "name": "unidad",
            "type": "str",
            "default": "''",
            "description": "Varies by group (e.g. \"Millones de USD\", \"Indice\", \"Porcentaje\"). Defaults to the first one available for the chosen frequency."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Time series data for one Banco Central del Ecuador (BCE) indicator group. Use search_indicadores_bce first to find the id_grupo. Frequency and unit vary by group (e.g. \"Mensual\"/\"Millones de USD\" for money supply, \"Trimestral\"/\"Porcentaje\" for GDP growth) — omit them to get the group's default, or check the error message for the valid options if you guess wrong."
      }
    ]
  },
  {
    "category": "Companies (Supercías)",
    "tools": [
      {
        "name": "search_companias",
        "description": "Search the Superintendency of Companies' directory (226k+, by name/RUC, province, legal status).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against company name or RUC (accent-insensitive)"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Optional province filter, e.g. \"PICHINCHA\" (substring match)"
          },
          {
            "name": "situacion_legal",
            "type": "str",
            "default": "''",
            "description": "Optional legal status filter, e.g. \"ACTIVA\", \"DISOLUCIÓN\""
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search Ecuador's company registry (Superintendencia de Compañías). Covers 226k+ companies with legal status, incorporation date, legal representative, registered capital, economic activity (CIIU) and address. Source is a daily-refreshed government export, cached up to 6h server-side — the first call after the cache expires can take ~30-40s to download and parse (~35 MB, 226k rows)."
      },
      {
        "name": "get_compania_info",
        "description": "Full company profile by RUC: legal representative, subscribed capital, CIIU code, address.",
        "params": [
          {
            "name": "ruc",
            "type": "str",
            "default": null,
            "description": "The company's 13-digit RUC"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get full registry details for a company by RUC (Superintendencia de Compañías). Returns legal status, incorporation date, legal representative, registered capital, economic activity (CIIU), address and last balance-sheet year. Use search_companias first if you don't have the exact RUC."
      },
      {
        "name": "search_ranking",
        "description": "Rank/filter companies by financial indicators (year, CIIU, any column); requires the local financial database build.",
        "params": [
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Optional fiscal year filter."
          },
          {
            "name": "ciiu_n1",
            "type": "str",
            "default": "''",
            "description": "Optional CIIU level-1 filter, e.g. \"C\", \"G\", \"I\"."
          },
          {
            "name": "order_by",
            "type": "str",
            "default": "'posicion_general'",
            "description": "Column to sort ascending by (e.g. \"posicion_general\", \"activos\", \"roe\"). Falls back to posicion_general if unknown."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)."
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Rank/filter Supercías companies by financial indicators for a fiscal year. Filter by year and/or CIIU level-1 economic activity (single letter, e.g. \"C\" for manufacturing), sorted by any indicator column (defaults to the dataset's own precomputed posicion_general). Each result includes the company's nombre/ruc alongside its financials. Covers only the last few cached fiscal years, not the full history. Use get_financials for one company's full detail, or get_compania_info for legal/registry data (address, legal representative, etc.)."
      },
      {
        "name": "get_financials",
        "description": "A company's financial history by filing number or RUC: revenue, assets, equity, ~38 ratios (liquidity, leverage, profitability).",
        "params": [
          {
            "name": "expediente_or_ruc",
            "type": "str",
            "default": null,
            "description": "The company's Supercías \"expediente\" number (from search_companias/get_compania_info) or its 13-digit RUC."
          },
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Optional single fiscal year filter."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get a company's financial history from Supercías balance-sheet filings. Returns revenue, assets, equity, profit, employee count, and financial ratios (liquidity, leverage, profitability) per fiscal year. Covers only the last few fiscal years (whatever scripts/build_supercias_financials_db.py last cached), not the full history back to 2008. Complements get_compania_info, which has legal/registry data but no financials."
      },
      {
        "name": "search_auditores",
        "description": "Search the registry of authorized external auditors (1,447 firms/individuals, by name or ID, province).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against auditor name or identificación (RUC/cédula), accent-insensitive"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Optional province filter, e.g. \"PICHINCHA\" (substring match)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search Ecuador's registry of authorized external auditors (Superintendencia de Compañías). Covers the 1,447 firms/individuals licensed to act as external auditors, with their authorization resolution number/date and contact details. Source is a daily-refreshed government export, cached up to 6h server-side."
      },
      {
        "name": "get_auditor_info",
        "description": "Full profile of an external auditor by ID: authorization resolution, nationality, address, contact.",
        "params": [
          {
            "name": "identificacion",
            "type": "str",
            "default": null,
            "description": "The auditor's RUC or cédula"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get full registry details for an authorized external auditor by identificación (Superintendencia de Compañías). Returns authorization resolution number/date, nationality, address and contact details. Use search_auditores first if you don't have the exact identificación (RUC/cédula)."
      }
    ]
  },
  {
    "category": "Regulations & contracts",
    "tools": [
      {
        "name": "search_regulaciones",
        "description": "Search/list regulations on gob.ec (with Official Register reference).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Keywords (e.g. \"datos personales\", \"tránsito\", \"LOTAIP\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number when query is empty (1-based)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search or list regulations published on Ecuador's gob.ec portal. Includes agreements, regulations and related norms with Registro Oficial references when available. With a query, scans several pages client-side (the API has no native search). Without a query, returns a paginated list."
      },
      {
        "name": "get_regulacion_info",
        "description": "Detail of a regulation, plus a link to the PDF.",
        "params": [
          {
            "name": "regulacion_id",
            "type": "str",
            "default": null,
            "description": "Regulation ID (e.g. \"5051\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a regulation published on gob.ec. Returns type, Registro Oficial reference, description and PDF/archivo URL when available. Get the regulacion_id from search_regulaciones."
      },
      {
        "name": "search_contratos",
        "description": "Search public procurement processes (SERCOP/OCDS).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Keyword (min 3 chars), e.g. \"medicinas\", \"vialidad\", \"software\""
          },
          {
            "name": "year",
            "type": "int",
            "default": "0",
            "description": "Contract year (2015–current). 0 = current year + fallback"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Results page (1-based)"
          },
          {
            "name": "buyer",
            "type": "str",
            "default": "''",
            "description": "Optional buyer/institution keyword (min 3 chars)"
          },
          {
            "name": "supplier",
            "type": "str",
            "default": "''",
            "description": "Optional supplier keyword (min 3 chars)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search Ecuador public procurement procedures (SERCOP / OCDS open data). Useful for journalists, researchers and citizens looking for contracts, tenders, buyers or suppliers. Requires a keyword of at least 3 characters. Year defaults to the current calendar year and falls back to prior years when year=0. Results are cached ~30 minutes; on 429 the client cools down."
      },
      {
        "name": "get_contrato_info",
        "description": "Full OCDS record: buyer, tender, awards, contracts.",
        "params": [
          {
            "name": "ocid",
            "type": "str",
            "default": null,
            "description": "Open Contracting ID, e.g. \"ocds-5wno2w-001-LICO-GPLR-2020-2805\""
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get the full OCDS record for a public procurement procedure (SERCOP). Returns buyer, tender, awards, contracts and useful links when available. Get the ocid from search_contratos."
      }
    ]
  },
  {
    "category": "Risk & earthquakes",
    "tools": [
      {
        "name": "search_eventos_riesgo",
        "description": "COE emergency/risk events (landslides, floods, etc.).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text (sector, cause, description keywords)"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Province filter (e.g. \"Pichincha\")"
          },
          {
            "name": "canton",
            "type": "str",
            "default": "''",
            "description": "Canton filter (e.g. \"Quito\")"
          },
          {
            "name": "evento",
            "type": "str",
            "default": "''",
            "description": "Event type filter (e.g. \"Deslizamiento\", \"Inundación\", \"Aluvión\")"
          },
          {
            "name": "estado",
            "type": "str",
            "default": "''",
            "description": "Status filter (e.g. \"Seguimiento\", \"Cierre\")"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "15",
            "description": "Max events to return (default 15, max 100)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search emergency/risk events from Ecuador's SGR COE (Gestión de Riesgos). Returns recent recorded events such as landslides, floods, structural damage, with province/canton, status (Seguimiento/Cierre), impacts and description. Data comes from the public ArcGIS COE2 service (cached ~5 minutes)."
      },
      {
        "name": "list_sat_tsunami",
        "description": "Tsunami early-warning stations.",
        "params": [
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max stations to include in the response (default 30)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List tsunami early-warning SAT stations published by SGR (Gestión de Riesgos). Returns station codes and coordinates from the public SAT MapServer."
      },
      {
        "name": "search_sismos",
        "description": "Recent earthquakes from the Geophysical Institute's (IG-EPN) catalog: magnitude, depth, location, and review status.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text over location/id (e.g. \"Quito\", \"Esmeraldas\")"
          },
          {
            "name": "magnitud_minima",
            "type": "float",
            "default": "0.0",
            "description": "Minimum magnitude (e.g. 4.0)"
          },
          {
            "name": "dias",
            "type": "int",
            "default": "0",
            "description": "Only events from the last N days (0 = all available)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "15",
            "description": "Max events to return (default 15, max 100)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search recent earthquakes in Ecuador from the Instituto Geofísico (IG-EPN). Returns the IG-EPN seismic catalog feed: magnitude, depth, coordinates, local/UTC time, review status and nearest-place description, with a link to each event page. Data is cached ~2 minutes."
      }
    ]
  },
  {
    "category": "Exploration",
    "tools": [
      {
        "name": "search_organizations",
        "description": "Search across 98+ institutions that publish data (INEC, SRI, BCE, MSP, etc.).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Optional search term (e.g. \"salud\", \"SRI\", \"INEC\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1)"
          },
          {
            "name": "page_size",
            "type": "int",
            "default": "20",
            "description": "Results per page (default: 20, max: 100)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search for public institutions that publish data on Ecuador's open data portal. There are 98+ organizations including INEC, SRI, Ministerio de Salud, BCE, etc."
      },
      {
        "name": "get_organization_info",
        "description": "Info about an organization, with a list of its datasets.",
        "params": [
          {
            "name": "organization_id",
            "type": "str",
            "default": null,
            "description": "The organization slug (e.g. \"sri-servicio-de-rentas-internas\")"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a public institution and its published datasets. Returns the institution name, description, dataset count, and a list of its datasets."
      },
      {
        "name": "list_categories",
        "description": "Topic categories with dataset counts.",
        "params": [
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List all thematic categories of Ecuador's open data portal. Categories include: Salud, Educación, Economía y Finanzas, Seguridad y Defensa, Anticorrupción, Ambiente y Agua, Transporte, Turismo, and more. Each category shows the number of datasets it contains. Use the category 'name' field as the 'category' parameter in search_datasets to filter results by topic."
      },
      {
        "name": "get_category_info",
        "description": "Detail of a category and sample datasets.",
        "params": [
          {
            "name": "category",
            "type": "str",
            "default": null,
            "description": "Category slug/name from list_categories"
          },
          {
            "name": "include_datasets",
            "type": "bool",
            "default": "True",
            "description": "Include sample datasets in the category (default True)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get details for a thematic category (CKAN group) on Ecuador's open data portal. Use list_categories first to discover valid category IDs (the 'name' field, e.g. 'salud', 'educacion', 'economia-y-finanzas')."
      }
    ]
  }
]
