[
  {
    "category": "Unified entry points",
    "tools": [
      {
        "name": "list_capabilities",
        "description": "Summarizes sources, tools, prompts, and server limits.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Describe what this Ecuador MCP can do: sources, key tools, prompts and limits. Call this first when you are unsure which tool to use."
      },
      {
        "name": "search_ecuador",
        "description": "Searches datasets, orgs, procedures, regulations, contracts, and risk events all at once.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Search terms in Spanish (e.g. \"salud\", \"RUC\", \"medicinas\")"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "5",
            "description": "Max results per section (default 5, max 10)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Unified search across Ecuador open data, trámites, regulations, contracts and risk events. Use this as the first step for broad questions. Then drill down with get_dataset_info, query_resource_data, get_tramite_info, get_regulacion_info, get_contrato_info or search_eventos_riesgo."
      },
      {
        "name": "lookup_ubicacion",
        "description": "Provinces, cantons, and parishes (INEC code, region, population).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Name or code (e.g. \"Pichincha\", \"Cuenca\", \"Tumbaco\", \"170150\")"
          },
          {
            "name": "region",
            "type": "str",
            "default": "''",
            "description": "Optional natural region for provinces/cantons"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Optional province filter"
          },
          {
            "name": "canton",
            "type": "str",
            "default": "''",
            "description": "Optional canton filter (useful for parroquias)"
          },
          {
            "name": "nivel",
            "type": "str",
            "default": "'auto'",
            "description": "auto | provincia | canton | parroquia"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Look up Ecuador DPA geography: provinces, cantons and parroquias (INEC codes). - nivel='provincia' | 'canton' | 'parroquia' | 'auto' - For parroquias, filter with canton= and/or provincia= (recommended)"
      }
    ]
  },
  {
    "category": "Open Data",
    "tools": [
      {
        "name": "search_datasets",
        "description": "Search datasets by keyword. Supports filtering by category.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Search keywords (e.g. \"empleo\", \"salud\", \"presupuesto\", \"SRI recaudación\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1)"
          },
          {
            "name": "page_size",
            "type": "int",
            "default": "20",
            "description": "Results per page (default: 20, max: 100)"
          },
          {
            "name": "category",
            "type": "str",
            "default": "''",
            "description": "Optional category filter (e.g. \"sal\" for Salud, \"edu\" for Educación). Use list_categories to see all available categories."
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search for datasets on Ecuador's open data portal (www.datosabiertos.gob.ec). This is the starting point for exploring government data from 98+ public institutions. Use short, specific queries in Spanish for best results. Typical workflow: search_datasets → list_dataset_resources → preview_resource_data"
      },
      {
        "name": "list_recent_datasets",
        "description": "The most recently updated datasets on the portal.",
        "params": [
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default 1)"
          },
          {
            "name": "page_size",
            "type": "int",
            "default": "15",
            "description": "Results per page (default 15, max 50)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List the most recently updated datasets on Ecuador's open data portal. Useful to discover new or freshly refreshed government data without a specific keyword. Sorted by metadata_modified descending."
      },
      {
        "name": "get_dataset_info",
        "description": "Detailed dataset metadata: title, description, organization, tags, license, dates.",
        "params": [
          {
            "name": "dataset_id",
            "type": "str",
            "default": null,
            "description": "The dataset ID or slug (e.g. \"registro-estadistico-de-recursos-y-actividades-de-salud-2019\")"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed metadata about a specific dataset from Ecuador's open data portal. Returns title, description, organization, tags, resource count, creation/update dates, license, update frequency, the publisher's source URL (where the entity keeps the authoritative data, if any), and any custom metadata fields the publisher added."
      },
      {
        "name": "list_dataset_resources",
        "description": "Lists all files (resources) in a dataset with format, size, URL, and creation/modification dates.",
        "params": [
          {
            "name": "dataset_id",
            "type": "str",
            "default": null,
            "description": "The dataset ID or slug"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List all resources (files) in a dataset with their metadata. Returns resource ID, title, format, size, download URL, and creation/ last-modified dates for each file. The dates let you tell whether a dataset's files are periodic snapshots (compare last_modified across resources to find the most recent one) before reading any of them. Also flags groups of resources whose names look like a periodic series (e.g. weekly files) — that's only a hint that they might need combining or picking the latest one; it does not tell you which, since that depends on the dataset (some periodic files replace previous ones, others complement them). Next step: use preview_resource_data on a CSV resource to see its contents, or use get_resource_info for detailed metadata."
      },
      {
        "name": "get_resource_info",
        "description": "Detailed information about a specific file.",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "The resource UUID"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a specific resource (file) from Ecuador's open data portal. Returns format, size, MIME type, download URL, and dataset association. For CSV files, you can then use preview_resource_data to see the actual data."
      },
      {
        "name": "preview_resource_data",
        "description": "Preview of CSV/TSV, JSON/GeoJSON, or Excel (XLS/XLSX) as a table (max 5 MB).",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "The resource UUID (get it from list_dataset_resources)"
          },
          {
            "name": "rows",
            "type": "int",
            "default": "20",
            "description": "Number of data rows to preview (default: 20, max: 100)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Download and preview a resource from Ecuador's open data portal. Supports CSV/TSV, JSON/GeoJSON and Excel (XLSX). Returns the first N rows as a formatted table so the model can inspect data without a local download. Geometry/WKT columns are dropped from the table (they can be tens of KB per cell); CSV columns in European decimal notation (7.760,2) are normalized to standard notation (7760.2). Max download size: 5 MB. For large tabular DataStore resources prefer query_resource_data."
      },
      {
        "name": "download_resource",
        "description": "Downloads the raw file of a resource as base64 (max 5 MB), for formats that can't be previewed as a table (.rar, .tar.gz, etc.).",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "The resource UUID (get it from list_dataset_resources)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Download a resource's raw bytes from Ecuador's open data portal, base64-encoded. Use this for formats preview_resource_data can't parse into a table (.rar, .tar.gz, legacy .xls, or anything unrecognized) — it fetches the file as-is instead of trying to read it. Max size: 5 MB (same cap as preview_resource_data). Larger files come back with an error and the direct URL instead, since they'd be too big to embed in a response. Call with format=\"json\" to get the actual file content — the default format=\"text\" only confirms the download and tells you to retry with json; it never includes content_base64."
      },
      {
        "name": "query_resource_data",
        "description": "Tabular query via the CKAN DataStore (filters, text, pagination) without downloading the file.",
        "params": [
          {
            "name": "resource_id",
            "type": "str",
            "default": null,
            "description": "Resource UUID (from list_dataset_resources)"
          },
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Optional full-text search across datastore fields"
          },
          {
            "name": "filters_json",
            "type": "str",
            "default": "''",
            "description": "Optional JSON object of exact field filters, e.g. '{\"provincia\":\"Pichincha\"}'"
          },
          {
            "name": "rows",
            "type": "int",
            "default": "20",
            "description": "Number of records to return (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset (default 0)"
          },
          {
            "name": "sort",
            "type": "str",
            "default": "''",
            "description": "Optional sort expression, e.g. \"anio desc\""
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Query a tabular resource via CKAN DataStore without downloading the full file. Prefer this over preview_resource_data when the resource is in the DataStore (most CSV resources on the portal). Supports full-text query, JSON filters, sorting and pagination."
      },
      {
        "name": "detect_series_pattern",
        "description": "For datasets that publish one file per period: tells whether each new file replaces the previous ones (cumulative) or complements them (incremental).",
        "params": [
          {
            "name": "dataset_id",
            "type": "str",
            "default": null,
            "description": "The dataset ID or slug"
          },
          {
            "name": "resource_id_new",
            "type": "str | None",
            "default": "None",
            "description": "Optional -- newer resource ID to compare (auto-detected from the dataset's periodic-name group if omitted)"
          },
          {
            "name": "resource_id_old",
            "type": "str | None",
            "default": "None",
            "description": "Optional -- older resource ID to compare (auto-detected if omitted). Both resource_id_new/resource_id_old must be given together, or neither."
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "When a dataset publishes one file per period (weekly/monthly), it's not always clear whether each new file replaces the previous ones (cumulative -- only the newest file matters) or complements them (incremental -- every file must be combined). Downloads the two most recent resources from the periodic-name group list_dataset_resources already detects, finds a date/period-like column by header name (fecha, mes, año, periodo, semana, ...), and compares which period values appear in both files. High overlap → cumulative; near-zero overlap → incremental; if no period column is found or the overlap is ambiguous, returns indeterminado rather than guessing. A heuristic over a sample of up to 500 rows per file -- confirm with preview_resource_data if the result is close to the threshold."
      },
      {
        "name": "search_sri_datasets",
        "description": "Search across ~130 SRI files published outside the CKAN portal: RUC registry by province, tax collection, sales/purchases, vehicles, CEL, data dictionaries.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the file's label or URL (accent-insensitive), e.g. \"recaudación\", \"vehiculos\", \"Pichincha\". Empty returns all files."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max results (default 30, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search the SRI's open datasets page (sri.gob.ec/datasets) for direct file links. Covers ~130 CSV/XLSX/ZIP files not reachable through search_datasets (they live on the SRI's own site, not the CKAN DataStore): RUC catastro by province, recaudación by year, ventas/compras, vehículos nuevos, comprobantes electrónicos (CEL), inscripciones/cierres, plus variable dictionaries (*_DD.xlsx). Returns direct URLs, not the file contents — download them yourself or via download_resource."
      }
    ]
  },
  {
    "category": "Government procedures",
    "tools": [
      {
        "name": "search_tramites",
        "description": "Search Ecuadorian government procedures (ID card, passport, RUC, driver's license, etc.).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Keywords to filter results (e.g. \"RUC\", \"inscripción RUC persona natural\"). Each word must appear in the trámite name or description."
          },
          {
            "name": "institution_id",
            "type": "str",
            "default": "''",
            "description": "Institution ID (strongly recommended for relevant results)"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1). Ignored when query is provided (all pages are searched automatically)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search for government procedures (trámites) on Ecuador's official portal gob.ec. Best approach: provide an institution_id to get that institution's trámites. Add a query to filter results by keyword (filters by name and description). When query is provided with institution_id, ALL pages of that institution's trámites are fetched and filtered, so you always get the most relevant results. Common institution IDs: - \"8\" = SRI (Servicio de Rentas Internas) — impuestos, RUC, facturación - \"23\" = Registro Civil — cédula, partidas de nacimiento - \"62\" = ANT — licencias de conducir, matriculación vehicular - \"16\" = Ministerio de Relaciones Exteriores — pasaportes, apostilla - \"5\" = IESS — seguro social, pensiones, fondos de reserva Use list_instituciones to find more institution IDs."
      },
      {
        "name": "get_tramite_info",
        "description": "Full detail: requirements, procedure, cost, estimated time.",
        "params": [
          {
            "name": "tramite_id",
            "type": "str",
            "default": null,
            "description": "The procedure ID (e.g. \"18009\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a specific government procedure (trámite). Returns the procedure name, description, requirements, beneficiaries, cost, and the institution responsible. Get the tramite_id from search_tramites."
      },
      {
        "name": "get_tramite_estadisticas",
        "description": "Monthly usage/complaint transparency series for one trámite, since 2021.",
        "params": [
          {
            "name": "tramite_id",
            "type": "str",
            "default": null,
            "description": "The procedure ID (e.g. \"11752\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Monthly usage and complaint series (atenciones/quejas) for one specific trámite. Different from get_tramite_info: this is how many people were actually attended each month, and how many complaints were filed, since gob.ec started publishing this (mid-2021). Get tramite_id from search_tramites. There is no bulk endpoint -- this fetches one trámite's series at a time (currently a few dozen months, no pagination needed)."
      },
      {
        "name": "list_instituciones",
        "description": "List Ecuador's public institutions.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Optional search term (e.g. \"SRI\", \"salud\", \"rentas\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1, only used without query)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List or search public institutions registered on Ecuador's gob.ec portal. If a query is provided, searches across all institutions by name or acronym. Without a query, returns a paginated list. Common institutions: SRI (ID: 8), Registro Civil (ID: 23), ANT (ID: 62), Cancillería (ID: 16), IESS (ID: 5), Ministerio de Salud, INEC, BCE."
      },
      {
        "name": "get_institucion_info",
        "description": "Detail of an institution (sector, website, description).",
        "params": [
          {
            "name": "institucion_id",
            "type": "str",
            "default": null,
            "description": "Institution ID (e.g. \"8\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a public institution registered on gob.ec. Returns name, acronym, sector, description, website and portal URL. Get the institucion_id from list_instituciones. Common IDs: SRI=8, IESS=5, Registro Civil=23, ANT=62, Cancillería=16."
      }
    ]
  },
  {
    "category": "ANDA (INEC)",
    "tools": [
      {
        "name": "search_anda",
        "description": "Search INEC's ANDA catalog of surveys and censuses (NADA/IHSN). Indicates whether each survey has downloadable microdata.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Search keywords (e.g. \"empleo\", \"REEM\", \"censo agropecuario\")"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "10",
            "description": "Max results (default: 10, max: 50)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search INEC's ANDA catalog (anda.inec.gob.ec) of surveys and censuses. ANDA runs on NADA (World Bank/IHSN), separate from datosabiertos.gob.ec. It catalogs 437+ INEC surveys/censuses with metadata (title, year, authoring entity). Not every entry has downloadable microdata — many are aggregate-only publications (e.g. price indices); each result says so. Follow up with get_anda_survey_info(idno) for full metadata on one survey."
      },
      {
        "name": "get_anda_survey_info",
        "description": "Full metadata for an ANDA survey: summary, variables, confidentiality, and contact.",
        "params": [
          {
            "name": "idno",
            "type": "str",
            "default": null,
            "description": "Survey identifier from search_anda (the \"idno\" field)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get full metadata for one ANDA (INEC) survey/census by its idno. Get the idno from search_anda results — it's the string identifier (e.g. \"IDD-ECU-INEC-CGTPE-DECON-ENESEM-2023-v1.3\"), not the numeric id. Returns scope/abstract, variable count, whether microdata is actually downloadable, confidentiality terms, and a contact email for requesting restricted data when it isn't."
      },
      {
        "name": "download_anda_microdata",
        "description": "Direct download links for an ANDA survey's microdata files.",
        "params": [
          {
            "name": "idno",
            "type": "str",
            "default": null,
            "description": "Survey identifier from search_anda (the \"idno\" field)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get direct download links for an ANDA survey's microdata files. Automates ANDA's one-click usage-terms step (research/statistical use only, no re-identifying respondents, cite the source — see get_anda_survey_info for the full text) to reveal the file list. Check get_anda_survey_info first: aggregate-only surveys (microdatos_disponibles false) have no files here. Returns direct URLs, not the file contents — the files are typically multi-MB ZIPs (SPSS .sav inside), too large to embed in a tool result. Download them with the returned URL."
      }
    ]
  },
  {
    "category": "Macroeconomics (BCE)",
    "tools": [
      {
        "name": "search_indicadores_bce",
        "description": "Search the Central Bank of Ecuador's statistical catalog (monetary/financial, public finance, external sector, real sector).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the group's description, its section/subsection, and its individual series labels (accent-insensitive)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search the Banco Central del Ecuador (BCE) statistical catalog. Covers monetary/financial statistics, public finances, external sector (foreign trade), and real sector (GDP, employment/ unemployment, consumer confidence, oil production, cement production, and more) — each result is an indicator group (id_grupo) whose time series data you fetch with get_indicador_bce. Some groups hold a single series, others dozens under a broader title (e.g. \"desempleo\" is a series inside the \"mercado laboral\" group, not a group of its own — matched here too, not just group titles). Does NOT cover Ecuador's own inflation (CPI) or poverty statistics — those are INEC's domain, try search_anda instead. First call after the cache expires (24h) takes ~10-15s: matching against series names, not just group titles, means fetching every group's metadata (~78 groups) once to build the search index. Cached after that."
      },
      {
        "name": "get_indicador_bce",
        "description": "Time series for an indicator by id_grupo: configurable period, frequency, and unit (defaults set per group).",
        "params": [
          {
            "name": "id_grupo",
            "type": "int",
            "default": null,
            "description": "The group id from search_indicadores_bce."
          },
          {
            "name": "desde",
            "type": "str",
            "default": "''",
            "description": "Start period as YYYY-MM. Defaults to the earliest available period for the chosen frequency."
          },
          {
            "name": "hasta",
            "type": "str",
            "default": "''",
            "description": "End period as YYYY-MM. Defaults to the latest available period for the chosen frequency."
          },
          {
            "name": "frecuencia",
            "type": "str",
            "default": "''",
            "description": "Semanal | Mensual | Trimestral | Anual. Defaults to the group's first available frequency."
          },
          {
            "name": "unidad",
            "type": "str",
            "default": "''",
            "description": "Varies by group (e.g. \"Millones de USD\", \"Indice\", \"Porcentaje\"). Defaults to the first one available for the chosen frequency."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Time series data for one Banco Central del Ecuador (BCE) indicator group. Use search_indicadores_bce first to find the id_grupo. Frequency and unit vary by group (e.g. \"Mensual\"/\"Millones de USD\" for money supply, \"Trimestral\"/\"Porcentaje\" for GDP growth) — omit them to get the group's default, or check the error message for the valid options if you guess wrong."
      }
    ]
  },
  {
    "category": "Companies (Supercías)",
    "tools": [
      {
        "name": "search_companias",
        "description": "Search the Superintendency of Companies' directory (226k+, by name/RUC, province, legal status).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against company name or RUC (accent-insensitive)"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Optional province filter, e.g. \"PICHINCHA\" (substring match)"
          },
          {
            "name": "situacion_legal",
            "type": "str",
            "default": "''",
            "description": "Optional legal status filter, e.g. \"ACTIVA\", \"DISOLUCIÓN\""
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search Ecuador's company registry (Superintendencia de Compañías). Covers 226k+ companies with legal status, incorporation date, legal representative, registered capital, economic activity (CIIU) and address. Source is a daily-refreshed government export, cached up to 6h server-side — the first call after the cache expires can take ~30-40s to download and parse (~35 MB, 226k rows)."
      },
      {
        "name": "get_compania_info",
        "description": "Full company profile by RUC: legal representative, subscribed capital, CIIU code, address.",
        "params": [
          {
            "name": "ruc",
            "type": "str",
            "default": null,
            "description": "The company's 13-digit RUC"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get full registry details for a company by RUC (Superintendencia de Compañías). Returns legal status, incorporation date, legal representative, registered capital, economic activity (CIIU), address and last balance-sheet year. Use search_companias first if you don't have the exact RUC."
      },
      {
        "name": "search_ranking",
        "description": "Rank/filter companies by financial indicators (year, CIIU, any column); requires the local financial database build.",
        "params": [
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Optional fiscal year filter."
          },
          {
            "name": "ciiu_n1",
            "type": "str",
            "default": "''",
            "description": "Optional CIIU level-1 filter, e.g. \"C\", \"G\", \"I\"."
          },
          {
            "name": "order_by",
            "type": "str",
            "default": "'posicion_general'",
            "description": "Column to sort ascending by (e.g. \"posicion_general\", \"activos\", \"roe\"). Falls back to posicion_general if unknown."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)."
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Rank/filter Supercías companies by financial indicators for a fiscal year. Filter by year and/or CIIU level-1 economic activity (single letter, e.g. \"C\" for manufacturing), sorted by any indicator column (defaults to the dataset's own precomputed posicion_general). Each result includes the company's nombre/ruc alongside its financials. Covers only the last few cached fiscal years, not the full history. Use get_financials for one company's full detail, or get_compania_info for legal/registry data (address, legal representative, etc.)."
      },
      {
        "name": "get_financials",
        "description": "A company's financial history by filing number or RUC: revenue, assets, equity, ~38 ratios (liquidity, leverage, profitability).",
        "params": [
          {
            "name": "expediente_or_ruc",
            "type": "str",
            "default": null,
            "description": "The company's Supercías \"expediente\" number (from search_companias/get_compania_info) or its 13-digit RUC."
          },
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Optional single fiscal year filter."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get a company's financial history from Supercías balance-sheet filings. Returns revenue, assets, equity, profit, employee count, and financial ratios (liquidity, leverage, profitability) per fiscal year. Covers only the last few fiscal years (whatever scripts/build_supercias_financials_db.py last cached), not the full history back to 2008. Complements get_compania_info, which has legal/registry data but no financials."
      },
      {
        "name": "search_auditores",
        "description": "Search the registry of authorized external auditors (1,447 firms/individuals, by name or ID, province).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against auditor name or identificación (RUC/cédula), accent-insensitive"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Optional province filter, e.g. \"PICHINCHA\" (substring match)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search Ecuador's registry of authorized external auditors (Superintendencia de Compañías). Covers the 1,447 firms/individuals licensed to act as external auditors, with their authorization resolution number/date and contact details. Source is a daily-refreshed government export, cached up to 6h server-side."
      },
      {
        "name": "get_auditor_info",
        "description": "Full profile of an external auditor by ID: authorization resolution, nationality, address, contact.",
        "params": [
          {
            "name": "identificacion",
            "type": "str",
            "default": null,
            "description": "The auditor's RUC or cédula"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get full registry details for an authorized external auditor by identificación (Superintendencia de Compañías). Returns authorization resolution number/date, nationality, address and contact details. Use search_auditores first if you don't have the exact identificación (RUC/cédula)."
      }
    ]
  },
  {
    "category": "Regulations & contracts",
    "tools": [
      {
        "name": "search_regulaciones",
        "description": "Search/list regulations on gob.ec (with Official Register reference).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Keywords (e.g. \"datos personales\", \"tránsito\", \"LOTAIP\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number when query is empty (1-based)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search or list regulations published on Ecuador's gob.ec portal. Includes agreements, regulations and related norms with Registro Oficial references when available. With a query, scans several pages client-side (the API has no native search). Without a query, returns a paginated list."
      },
      {
        "name": "get_regulacion_info",
        "description": "Detail of a regulation, plus a link to the PDF.",
        "params": [
          {
            "name": "regulacion_id",
            "type": "str",
            "default": null,
            "description": "Regulation ID (e.g. \"5051\")"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a regulation published on gob.ec. Returns type, Registro Oficial reference, description and PDF/archivo URL when available. Get the regulacion_id from search_regulaciones."
      },
      {
        "name": "search_contratos",
        "description": "Search public procurement processes (SERCOP/OCDS).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Keyword (min 3 chars), e.g. \"medicinas\", \"vialidad\", \"software\""
          },
          {
            "name": "year",
            "type": "int",
            "default": "0",
            "description": "Contract year (2015–current). 0 = current year + fallback"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Results page (1-based)"
          },
          {
            "name": "buyer",
            "type": "str",
            "default": "''",
            "description": "Optional buyer/institution keyword (min 3 chars)"
          },
          {
            "name": "supplier",
            "type": "str",
            "default": "''",
            "description": "Optional supplier keyword (min 3 chars)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search Ecuador public procurement procedures (SERCOP / OCDS open data). Useful for journalists, researchers and citizens looking for contracts, tenders, buyers or suppliers. Requires a keyword of at least 3 characters. Year defaults to the current calendar year and falls back to prior years when year=0. Results are cached ~30 minutes; on 429 the client cools down."
      },
      {
        "name": "get_contrato_info",
        "description": "Full OCDS record: buyer, tender, awards, contracts.",
        "params": [
          {
            "name": "ocid",
            "type": "str",
            "default": null,
            "description": "Open Contracting ID, e.g. \"ocds-5wno2w-001-LICO-GPLR-2020-2805\""
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get the full OCDS record for a public procurement procedure (SERCOP). Returns buyer, tender, awards, contracts and useful links when available. Get the ocid from search_contratos."
      }
    ]
  },
  {
    "category": "Risk & earthquakes",
    "tools": [
      {
        "name": "search_eventos_riesgo",
        "description": "COE emergency/risk events (landslides, floods, etc.).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text (sector, cause, description keywords)"
          },
          {
            "name": "provincia",
            "type": "str",
            "default": "''",
            "description": "Province filter (e.g. \"Pichincha\")"
          },
          {
            "name": "canton",
            "type": "str",
            "default": "''",
            "description": "Canton filter (e.g. \"Quito\")"
          },
          {
            "name": "evento",
            "type": "str",
            "default": "''",
            "description": "Event type filter (e.g. \"Deslizamiento\", \"Inundación\", \"Aluvión\")"
          },
          {
            "name": "estado",
            "type": "str",
            "default": "''",
            "description": "Status filter (e.g. \"Seguimiento\", \"Cierre\")"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "15",
            "description": "Max events to return (default 15, max 100)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search emergency/risk events from Ecuador's SGR COE (Gestión de Riesgos). Returns recent recorded events such as landslides, floods, structural damage, with province/canton, status (Seguimiento/Cierre), impacts and description. Data comes from the public ArcGIS COE2 service (cached ~5 minutes)."
      },
      {
        "name": "list_sat_tsunami",
        "description": "Tsunami early-warning stations.",
        "params": [
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max stations to include in the response (default 30)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List tsunami early-warning SAT stations published by SGR (Gestión de Riesgos). Returns station codes and coordinates from the public SAT MapServer."
      },
      {
        "name": "search_sismos",
        "description": "Recent earthquakes from the Geophysical Institute's (IG-EPN) catalog: magnitude, depth, location, and review status.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text over location/id (e.g. \"Quito\", \"Esmeraldas\")"
          },
          {
            "name": "magnitud_minima",
            "type": "float",
            "default": "0.0",
            "description": "Minimum magnitude (e.g. 4.0)"
          },
          {
            "name": "dias",
            "type": "int",
            "default": "0",
            "description": "Only events from the last N days (0 = all available)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "15",
            "description": "Max events to return (default 15, max 100)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search recent earthquakes in Ecuador from the Instituto Geofísico (IG-EPN). Returns the IG-EPN seismic catalog feed: magnitude, depth, coordinates, local/UTC time, review status and nearest-place description, with a link to each event page. Data is cached ~2 minutes."
      },
      {
        "name": "search_sgr_sitreps",
        "description": "Historical archive of SGR adverse-event dossiers (2016-2026).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the event's titulo, estado, or descripcion, e.g. \"terremoto\", \"en curso\", \"manabi\". Empty returns all events."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists SGR's SITREP (\"Informes de Situación\") archive of adverse-event dossiers, 2016-2026. A different source from search_eventos_riesgo, which reads a live ArcGIS snapshot of current events only. Lists about 54 multi-year dossiers — earthquakes, forest-fire seasons, rainy seasons, landslides, volcanic activity — each with a status and its own page. Follow up with get_sgr_sitrep_archivos(url)."
      },
      {
        "name": "get_sgr_sitrep_archivos",
        "description": "SITREP PDF reports for one SGR adverse event.",
        "params": [
          {
            "name": "evento_url",
            "type": "str",
            "default": null,
            "description": "An event URL from search_sgr_sitreps' url field (must be on gestionderiesgos.gob.ec)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the SITREP PDF report links published on one SGR adverse-event page. Returns each report's grupo (the page's own section heading, e.g. national vs. provincial vs. cantonal), título, and direct PDF URL. A long-running event can carry 700+ PDFs; a closed single-incident event usually has far fewer."
      },
      {
        "name": "list_sgr_biblioteca_categorias",
        "description": "Categories of SGR's document library (~1660 documents).",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists SGR's Biblioteca document-library categories — resolutions, contingency plans, threat maps, tsunami evacuation routes, and more, about 1660 documents across 19 top-level categories. A different source from search_eventos_riesgo/search_sgr_sitreps: a standing document library, not event reports. A real share of the library's links return 404 — treat this as a candidate catalog, not a guarantee."
      },
      {
        "name": "get_sgr_biblioteca_categoria_archivos",
        "description": "Documents in one SGR library category.",
        "params": [
          {
            "name": "categoria",
            "type": "str",
            "default": null,
            "description": "A category \"id\" or \"nombre\" from list_sgr_biblioteca_categorias (nombre match is accent/case-insensitive)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists one SGR Biblioteca top-level category's documents. Returns each document's subgrupo (nested sub-category, when present), título, id, and direct URL. Format is reported as unknown for every entry because the download link carries no file extension."
      },
      {
        "name": "search_inamhi_capas",
        "description": "Layer catalog of INAMHI's geoportal (meteorology and hydrology).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the layer's name, title, or abstract, e.g. \"precipitacion\", \"cuencas\", \"anomalias\". Empty returns all layers."
          },
          {
            "name": "solo_wfs",
            "type": "bool",
            "default": "False",
            "description": "If True, only return layers with WFS (attribute data) available."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Searches INAMHI's geoportal layer catalog — a GeoNode-backed GeoServer exposing Ecuador's meteorology/hydrology institute's spatial layers via WMS/WFS. 222 layers confirmed live: precipitation climate normals, daily rainfall-anomaly composites, WRF numerical weather model output grids, watershed and administrative boundaries. 199 of the 222 also expose WFS; the other 23 are raster-only. This catalog does not include a per-station raw observation layer — everything is polygon-aggregated."
      },
      {
        "name": "get_inamhi_capa_datos",
        "description": "Sample of real attribute data from an INAMHI geoportal layer.",
        "params": [
          {
            "name": "layer_name",
            "type": "str",
            "default": null,
            "description": "Layer name from search_inamhi_capas, including the \"geonode:\" workspace prefix, e.g. \"geonode:regiones_precip\"."
          },
          {
            "name": "count",
            "type": "int",
            "default": "5",
            "description": "Number of features to fetch (1-20, default 5)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetches a small, bounded sample of a WFS-enabled INAMHI geoportal layer's real feature attributes (via WFS GetFeature, JSON output). Use search_inamhi_capas first to find a layer name and check wfs_disponible=True (raster-only layers, e.g. WRF weather-model grids, will raise an error here). Not a spatial query engine."
      }
    ]
  },
  {
    "category": "Exploration",
    "tools": [
      {
        "name": "search_organizations",
        "description": "Search across 98+ institutions that publish data (INEC, SRI, BCE, MSP, etc.).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Optional search term (e.g. \"salud\", \"SRI\", \"INEC\")"
          },
          {
            "name": "page",
            "type": "int",
            "default": "1",
            "description": "Page number (1-based, default: 1)"
          },
          {
            "name": "page_size",
            "type": "int",
            "default": "20",
            "description": "Results per page (default: 20, max: 100)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Search for public institutions that publish data on Ecuador's open data portal. There are 98+ organizations including INEC, SRI, Ministerio de Salud, BCE, etc."
      },
      {
        "name": "get_organization_info",
        "description": "Info about an organization, with a list of its datasets.",
        "params": [
          {
            "name": "organization_id",
            "type": "str",
            "default": null,
            "description": "The organization slug (e.g. \"sri-servicio-de-rentas-internas\")"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get detailed information about a public institution and its published datasets. Returns the institution name, description, dataset count, and a list of its datasets."
      },
      {
        "name": "list_categories",
        "description": "Topic categories with dataset counts.",
        "params": [
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "List all thematic categories of Ecuador's open data portal. Categories include: Salud, Educación, Economía y Finanzas, Seguridad y Defensa, Anticorrupción, Ambiente y Agua, Transporte, Turismo, and more. Each category shows the number of datasets it contains. Use the category 'name' field as the 'category' parameter in search_datasets to filter results by topic."
      },
      {
        "name": "get_category_info",
        "description": "Detail of a category and sample datasets.",
        "params": [
          {
            "name": "category",
            "type": "str",
            "default": null,
            "description": "Category slug/name from list_categories"
          },
          {
            "name": "include_datasets",
            "type": "bool",
            "default": "True",
            "description": "Include sample datasets in the category (default True)"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default, www.datosabiertos.gob.ec) or \"cuenca\" (cuencaendatos.cuenca.gob.ec, independent municipal portal)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Output format: `text` (default) or `json`."
          }
        ],
        "long_description": "Get details for a thematic category (CKAN group) on Ecuador's open data portal. Use list_categories first to discover valid category IDs (the 'name' field, e.g. 'salud', 'educacion', 'economia-y-finanzas')."
      }
    ]
  },
  {
    "category": "BCE and specialized series",
    "tools": [
      {
        "name": "audit_bce_catalog",
        "description": "Audit the live BCEData catalogue and report its coverage.",
        "params": [
          {
            "name": "incluir_grupos",
            "type": "bool",
            "default": "False",
            "description": "Include every discovered group's metadata in the response. Defaults to false to keep mobile responses small."
          },
          {
            "name": "guardar_snapshot",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "comparar_anterior",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "auditar_grid",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Audit the live BCEData catalogue and report its coverage. Fetches the catalogue tree and every indicator group's metadata. The report counts groups, series, sections and failed metadata requests. Set incluir_grupos=true to include the complete per-group inventory. Use get_indicador_bce for the actual values of a selected group. Set guardar_snapshot=true to persist the audit under BCE_CATALOG_SNAPSHOT_DIR (or data/bce_catalog_snapshots by default). Set comparar_anterior=true to compare the current catalog with the last complete saved audit. Set auditar_grid=true to probe one latest period for every discovered frequency/unit combination. The value audit is bounded and persisted separately when guardar_snapshot=true."
      },
      {
        "name": "compare_bce_sources",
        "description": "Build a cautious BCEData ↔ IEM candidate-equivalence map.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "100",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "historico",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Parameter documented in the source code."
          }
        ],
        "long_description": "Build a cautious BCEData ↔ IEM candidate-equivalence map. Matching uses normalized group/series/table labels. It reports candidate overlaps and source-only entries, but does not claim that two labels have identical definitions. Confirm unit, frequency, coverage, values and revisions before combining them."
      },
      {
        "name": "get_bce_iem_table",
        "description": "Inspect one official XLSX table from the latest BCE IEM bulletin.",
        "params": [
          {
            "name": "table_id",
            "type": "str",
            "default": null,
            "description": "Parameter documented in the source code."
          },
          {
            "name": "desde",
            "type": "str",
            "default": "''",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "hasta",
            "type": "str",
            "default": "''",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "boletin_numero",
            "type": "int",
            "default": "0",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "rows",
            "type": "int",
            "default": "20",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Parameter documented in the source code."
          }
        ],
        "long_description": "Inspect one official XLSX table from the latest BCE IEM bulletin. Get table_id from search_bce_iem. Tables with the common BCE layout (periods across columns and variables down rows) return structured, date-filterable series. Other layouts return a faithful preview rather than silently guessing columns. Always includes the BCE file and bulletin URLs for verification. Pass boletin_numero to retrieve a historical version returned by search_bce_iem(historico=true)."
      },
      {
        "name": "get_bce_indicador_diario",
        "description": "Fetch one BCE daily/monthly indicator's time series (e.g. Riesgo País).",
        "params": [
          {
            "name": "archivo",
            "type": "str",
            "default": null,
            "description": "A file name from list_bce_indicadores_diarios."
          },
          {
            "name": "codigo",
            "type": "str",
            "default": null,
            "description": "A \"Código Variable Dinámica\" from that same archivo."
          },
          {
            "name": "ultimos_n",
            "type": "int",
            "default": "30",
            "description": "Most recent N observations when no date range is given."
          },
          {
            "name": "desde",
            "type": "str",
            "default": "''",
            "description": "Optional start date YYYY-MM-DD."
          },
          {
            "name": "hasta",
            "type": "str",
            "default": "''",
            "description": "Optional end date YYYY-MM-DD."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetch one BCE daily/monthly indicator's time series (e.g. Riesgo País), bounded to a window — never the full series (some run 7,000+ observations). Get archivo/codigo from list_bce_indicadores_diarios first — a codigo only means one thing within its own archivo. Without desde/hasta, returns the most recent ultimos_n observations (capped at 366). With desde/hasta (YYYY-MM-DD, inclusive), returns that date range instead, also capped at 366 rows (most recent within the range if it's larger). The response always includes rango_completo (the series' true full date range and row count) so the window returned is never mistaken for the whole series. Each point in \"datos\" has \"valor\" (one number) for almost every indicator, except Inflación (datos_ipc.json), which has no single value — its points carry \"valores\": {\"Mensual\":..., \"Anual\":..., \"Acumulada\":...} instead."
      },
      {
        "name": "list_bce_indicadores_diarios",
        "description": "List BCE's family of daily/monthly \"indicador\" widgets published outside both BCEData and IEM (contenido.bce.fin.ec's Highcharts widget pages) — includes Riesgo País (EMBI) as a genuine daily series back to 2004, which BCEData only exposes as a monthly end-of-period aggregate.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List BCE's family of daily/monthly \"indicador\" widgets published outside both BCEData and IEM (contenido.bce.fin.ec's Highcharts widget pages) — includes Riesgo País (EMBI) as a genuine daily series back to 2004, which BCEData only exposes as a monthly end-of-period aggregate. Also covers: Precio del Oro, Petróleo WTI, Índice Dow Jones, Tasa SOFR/LIBOR, Ecuador sovereign bond prices (all daily); Sistema de Pagos/Cobros Interbancarios, Sistema de Pagos en Línea, Cámara de Compensación de Cheques (monthly); Producción Petrolera Nacional (daily); inflación, desempleo, confianza del consumidor, PIB (monthly/quarterly/annual, likely duplicates BCEData). The catalog is discovered live from each file's own data, not hardcoded — a \"codigo\" only means one thing within its own \"archivo\", not across files. Follow up with get_bce_indicador_diario(archivo, codigo) for one series."
      },
      {
        "name": "search_bce_iem",
        "description": "Search individual Excel tables in the BCE's latest IEM bulletin.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "historico",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "desde_anio",
            "type": "int",
            "default": "0",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "hasta_anio",
            "type": "int",
            "default": "0",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "guardar_catalogo",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "hash_archivos",
            "type": "bool",
            "default": "False",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "max_hash_archivos",
            "type": "int",
            "default": "5000",
            "description": "Parameter documented in the source code."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "Parameter documented in the source code."
          }
        ],
        "long_description": "Search individual Excel tables in the BCE's latest IEM bulletin. Use this for detailed BCE tables not conveniently available through search_indicadores_bce/BCEData: trade by country, debt, public finance by government level, oil, GDP breakdowns, and more. It lists tables from the current monthly bulletin; use get_bce_iem_table with a returned table_id to inspect one official XLSX file. Set historico=true (or provide desde_anio/hasta_anio) to search across all matching monthly bulletin pages; this is slower on the first call because the archive pages must be read. Set guardar_catalogo=true to persist the complete catalog assembled by this search under IEM_CATALOG_DIR (or data/iem_catalog_snapshots by default). Pagination still keeps the MCP response bounded. Set hash_archivos=true for an operator-style SHA-256 audit of the discovered XLSX files. This downloads complete files, so it is deliberately opt-in and bounded by max_hash_archivos."
      },
      {
        "name": "search_bce_remesas",
        "description": "List BCE's Remesas de Trabajadores (worker remittances) direct file links.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the file's label or URL (accent-insensitive), e.g. \"historica\", \"entidad\", \"bdd\". Empty returns all files."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List BCE's Remesas de Trabajadores (worker remittances) direct file links. A dedicated series separate from BCEData/IEM (search_indicadores_bce, search_bce_iem): the aggregate flow series, the full historical series, a methodology user note, and — since a July 2025 change to microdata-based collection — monthly aggregate and entity-level databases (BDD). Treat \"histórica\" (pre-change) and \"BDD\" (post-change) files as methodologically distinct series, not one continuous one — see the user note (Nota_al_usuario) for the comparability details. Returns direct URLs, not file contents — download them yourself or via download_resource."
      },
      {
        "name": "search_bce_publicaciones",
        "description": "Recent BCE publications (bulletins, reports, notices).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the publication's title (accent-insensitive). Empty returns all."
          },
          {
            "name": "formato",
            "type": "str",
            "default": "''",
            "description": "Exact match against the derived format — PDF, XLSX, XLS, CSV, ZIP, HTML. Empty returns all formats."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists recent Central Bank of Ecuador publications from its \"Últimas Publicaciones\" feed — monetary bulletins, interest-rate reports, sector-analysis bulletins, IEM release notices, and more. A dedicated catalog separate from BCEData/IEM (search_indicadores_bce, search_bce_iem) — most of what appears here has no numeric-series equivalent there. Only shows the rolling window the page itself displays (roughly the 30 most recent publications) — no pagination or date-range filter."
      },
      {
        "name": "search_bce_indices",
        "description": "BCE \"índice\" archive pages with a per-series historical file archive.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the page's title or URL. Empty returns all discovered pages."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists BCE \"índice\" archive pages — one per named publication series (sector bulletins for petroleum/mining/cement, trade price indices, EMOE confidence indices, FX buy/sell, balance of payments, weekly monetary bulletins, remittance bulletins), each with a year-by-year or week-by-week file archive going back as far as 2004. Returns summaries only — pass the returned pagina_id to get_bce_indice_archivo to read one series' file list."
      },
      {
        "name": "get_bce_indice_archivo",
        "description": "File archive for one BCE \"índice\" page.",
        "params": [
          {
            "name": "pagina_id",
            "type": "str",
            "default": null,
            "description": "The page's slug, from search_bce_indices' pagina_id field (e.g. \"boletin-analitico-del-sector-petrolero-indice\")."
          },
          {
            "name": "anio",
            "type": "int",
            "default": "0",
            "description": "Restrict to one calendar year. 0 returns all years found."
          },
          {
            "name": "max_archivos",
            "type": "int",
            "default": "30",
            "description": "Cap on returned files (most recent years first), 1-200. Use anio to reach further back."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Reads the file archive for one BCE \"índice\" page (from search_bce_indices). Each item is one published file: year, period label, a date when the source gives one (weekly series only), direct URL, and format."
      },
      {
        "name": "search_bce_precios_comex",
        "description": "BCE's disaggregated foreign-trade price indices.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the file's label, page title, or page id (accent-insensitive), e.g. \"importacion\", \"exportacion\". Empty returns all files."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists BCE's disaggregated foreign-trade price-index file links — import prices by economic-use category and export prices by individual product (crude oil, shrimp, banana, cacao, copper, gold, roses), each with separate price/value/volume sheets and variation sheets. Distinct from search_indicadores_bce's BCEData catalog (only three aggregate series) and from search_bce_indices (doesn't cover trade prices). Returns direct URLs, not file contents."
      }
    ]
  },
  {
    "category": "INEC: statistics, BIINEC, and census",
    "tools": [
      {
        "name": "get_inec_estadistica_files",
        "description": "List the direct file links published on one INEC statistical topic page.",
        "params": [
          {
            "name": "url",
            "type": "str",
            "default": null,
            "description": "A topic URL from search_inec_estadisticas's \"url\" field (must be on ecuadorencifras.gob.ec)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the direct file links published on one INEC statistical topic page. Get the url from search_inec_estadisticas. Returns technical bulletins, methodology, and historical series as direct PDF/XLSX/CSV/ZIP links — not the file contents. Use read_pdf on a .pdf link, or download it yourself for tabular formats."
      },
      {
        "name": "get_inec_publicacion_archivos",
        "description": "List the direct file links (PDF/XLSX/CSV/ZIP) embedded in one INEC publication found via search_inec_publicaciones.",
        "params": [
          {
            "name": "post",
            "type": "str",
            "default": null,
            "description": "Either the numeric \"id\" from search_inec_publicaciones, or the publication's full ecuadorencifras.gob.ec URL."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the direct file links (PDF/XLSX/CSV/ZIP) embedded in one INEC publication found via search_inec_publicaciones."
      },
      {
        "name": "search_biinec_extras",
        "description": "Check INEC's BIINEC (\"Banco de Datos Abiertos\") for data not found in search_anda or search_inec_estadisticas.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the registry's name/description (accent-insensitive). Empty returns the full curated list."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Check INEC's BIINEC (\"Banco de Datos Abiertos\") for data not found in search_anda or search_inec_estadisticas. Last resort for INEC queries: BIINEC (aplicaciones3.ecuadorencifras.gob.ec) mostly duplicates ANDA and Ecuador en Cifras, so this does NOT scrape or search BIINEC live — it's a small, manually-verified list of the handful of registries confirmed to exist only there (environmental/administrative modules, e.g. hazardous waste in health facilities, ENEMDU/ECV environmental modules). If your query matches nothing here, that does NOT mean BIINEC has nothing — it means it wasn't worth automating; search the site's own search box directly instead."
      },
      {
        "name": "search_censo_recursos",
        "description": "Search INEC's dedicated Census 2022 microsite (censoecuador.gob.ec) for direct microdata/methodology file links.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the file label, e.g. \"manlo\" (city-block level), \"sector\", \"2010\", \"spss\", \"diccionario\", \"metodologia\". Empty returns every file found."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max results (default 30, max 100)."
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Search INEC's dedicated Census 2022 microsite (censoecuador.gob.ec) for direct microdata/methodology file links. Far more complete than the general \"Censo de Población y Vivienda\" result from search_inec_estadisticas: full microdata at sector, cantón, and city-block (\"manzana\"/MANLOC) level in CSV, SPSS (SAV), and REDATAM formats, plus the 2010 and 2001 censuses recoded onto 2022 geography for comparability, variable dictionaries, and methodology/quality documents. Returns metadata and direct URLs only, never file contents -- these are large microdata archives (multi-hundred-MB in some cases), not previewable through this tool."
      },
      {
        "name": "search_inec_estadisticas",
        "description": "Search INEC's statistical topic pages (ecuadorencifras.gob.ec).",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the topic name (accent-insensitive), e.g. \"precios\", \"empleo\", \"pobreza\". Empty returns all topics."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max results (default 30, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Search INEC's statistical topic pages (ecuadorencifras.gob.ec). Covers ~75 topics INEC publishes (IPC, ENEMDU, ENSANUT, pobreza, comercio exterior, cuentas nacionales, construcción, censos...), separate from both search_datasets (CKAN) and search_anda (INEC's microdata archive). This is where the actual published aggregate series live — technical bulletins, methodology, and full historical series in Excel/CSV — for operations that ANDA only catalogs as metadata with no downloadable microdata (e.g. price indices). Follow up with get_inec_estadistica_files(url) for one topic's file links."
      },
      {
        "name": "search_inec_publicaciones",
        "description": "Search every post INEC has published on Ecuador en Cifras, via its public WordPress REST API -- always current, newest first.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against title/content (e.g. \"ENEMDU anual 2025\", \"inflación julio\", \"censo\"). Empty returns the most recent posts regardless of topic."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "20",
            "description": "Max results (default 20, max 100)."
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Search every post INEC has published on Ecuador en Cifras, via its public WordPress REST API -- always current, newest first. Complements search_inec_estadisticas: that tool's topic pages are curated landing pages that can go stale for months or years even while INEC keeps publishing the same operation (confirmed live: ENEMDU's old \"Empleo\" topic page stopped at April 2023 while INEC kept releasing it monthly under dated posts this tool finds). Use this tool for \"what's the latest bulletin/release for X\" questions. Use search_inec_estadisticas/get_inec_estadistica_files instead when you want one operation's fixed overview page rather than a specific dated release. Follow up with get_inec_publicacion_archivos(id) for one publication's file links."
      }
    ]
  },
  {
    "category": "SRI: RUC and tax collection",
    "tools": [
      {
        "name": "get_sri_ruc_info",
        "description": "Look up a taxpayer's public record by RUC in the SRI.",
        "params": [
          {
            "name": "ruc",
            "type": "str",
            "default": null,
            "description": "Exact 13-digit Ecuadorian RUC"
          },
          {
            "name": "include_establecimientos",
            "type": "bool",
            "default": "True",
            "description": "include registered establishments"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Look up a taxpayer's public record by RUC in the SRI. Returns legal name, status, type, economic activity, registration dates, and registered public establishments. Does not return individual tax declarations or amounts."
      },
      {
        "name": "search_sri_estadisticas_recaudacion",
        "description": "Search the SRI's \"Estadísticas de Recaudación\" page for direct file links.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the file's label or URL (accent-insensitive), e.g. \"recaudación\", \"provincia\", \"actividad economica\". Empty returns all files."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "30",
            "description": "Max results (default 30, max 100)"
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the matched set"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Search the SRI's \"Estadísticas de Recaudación\" page for direct file links. Separate from search_sri_datasets (raw yearly declaration exports): this covers monthly XLSX reports pre-aggregated by impuesto, provincia/cantón, and actividad económica (updated monthly), plus a ZIP of historical indicators, an annual PDF boletín técnico, and infografías. Returns direct URLs, not the file contents — download them yourself, via download_resource, or read_pdf for the PDFs."
      },
      {
        "name": "search_sri_ruc",
        "description": "Search SRI's RUC registry by legal name or trade name (partial text), without needing the exact RUC upfront.",
        "params": [
          {
            "name": "razon_social",
            "type": "str",
            "default": null,
            "description": "Text to search for (minimum 4 characters), e.g. \"BANANERA\""
          },
          {
            "name": "max_resultados",
            "type": "int",
            "default": "25",
            "description": "How many full-detail results to return (max 100)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Search SRI's RUC registry by legal name or trade name (partial text), without needing the exact RUC upfront. Complements get_sri_ruc_info (which requires the exact RUC) for the \"I know the company name, not its RUC\" case. Each result includes status, taxpayer type, régimen, economic activity, legal representatives, and whether it's a withholding agent/special taxpayer/ghost taxpayer — no declarations or individual tax amounts. The SRI caps this search at 100 matches per query; if total_reportado comes back as 100, there may be more taxpayers than are actually being returned — use more specific text to narrow it down."
      }
    ]
  },
  {
    "category": "SIPA, Contraloría, and Superbancos",
    "tools": [
      {
        "name": "get_contraloria_informe",
        "description": "Download and preview one Contraloría document (Datos Abiertos or Plan Anual de Control).",
        "params": [
          {
            "name": "informe_id",
            "type": "str",
            "default": null,
            "description": "An id from list_contraloria_informes"
          },
          {
            "name": "rows",
            "type": "int",
            "default": "50",
            "description": "Number of data rows to preview (default: 50, max: 200)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Download and preview one Contraloría document (Datos Abiertos or Plan Anual de Control). Get informe_id from list_contraloria_informes. Quarterly \"Datos Abiertos\" documents return one row per audit report approved that quarter, across every public institution in the country. \"Plan Anual de Control\" documents are PDFs (one per year); this returns their metadata and points you at read_pdf instead of a table."
      },
      {
        "name": "get_sipa_modulo_archivos",
        "description": "List the direct download links published on one SIPA statistics module.",
        "params": [
          {
            "name": "modulo",
            "type": "str",
            "default": null,
            "description": "A module key from list_sipa_modulos, e.g. \"economico\", \"productivo\", \"social\", \"censos\"."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the direct download links published on one SIPA statistics module. Get the modulo key from list_sipa_modulos. Returns each file's title, description, direct XLSX/XLS URL, and format — not the file contents. Files can be large (one confirmed at 41 MB); download the URL directly instead of routing it through preview_resource_data or download_resource, which cap at 5 MB."
      },
      {
        "name": "get_superbancos_seccion_archivos",
        "description": "List the direct download links published on one Superbancos statistics section page.",
        "params": [
          {
            "name": "seccion",
            "type": "str",
            "default": null,
            "description": "A section key from list_superbancos_secciones, e.g. \"boletines_financieros\", \"servicios_financieros\", \"informacion_historica\", \"calendario_estadistico\"."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the direct download links published on one Superbancos statistics section page. Get the seccion key from list_superbancos_secciones. Returns each file's grupo (table section, e.g. \"TARJETAS DE CRÉDITO\"), periodo (year label, when the table separates it from the file title), titulo, direct URL, and format — not the file contents. Files can be several MB; download the URL directly instead of routing it through preview_resource_data or download_resource, which cap at 5 MB. Coverage stops where the page's static archive tables stop — most recent years live in a client-side OneDrive widget this tool does not read. Do not present the newest \"periodo\"/\"titulo\" found here as the most recent data available from Superbancos."
      },
      {
        "name": "list_contraloria_informes",
        "description": "List Contraloría General del Estado's \"Datos Abiertos\" and \"Plan Anual de Control\" documents.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List Contraloría General del Estado's \"Datos Abiertos\" and \"Plan Anual de Control\" documents. \"Datos Abiertos\" are quarterly CSV exports of audit reports approved for ANY public institution in the country (Unidad de Control, Entidad, Diligencia, periodo, tipo de informe, N° de informe, fecha de aprobación) — much broader than any single-institution audit archive already covered elsewhere in this MCP. New quarters are added roughly every three months. \"Plan Anual de Control\" documents are one PDF per year (the approved annual control plan). Follow up with get_contraloria_informe(informe_id) for one document's parsed rows (CSV) or metadata + read_pdf pointer (PDF)."
      },
      {
        "name": "list_sipa_modulos",
        "description": "List SIPA's statistics-download modules (sipa.agricultura.gob.ec).",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List SIPA's statistics-download modules (sipa.agricultura.gob.ec). SIPA (Sistema de Información Pública Agropecuaria) is the Ministry of Agriculture, Livestock and Fisheries' statistics portal — distinct from MPCEIP (industry/trade). It publishes real Excel files with agropecuario price, trade, credit, production, and census series back to the early 2000s, organized into four modules: económico, productivo, social, and censos y registros administrativos. Follow up with get_sipa_modulo_archivos(modulo) for one module's file listing."
      },
      {
        "name": "list_superbancos_secciones",
        "description": "List the Superintendencia de Bancos statistics sections (superbancos.gob.ec/estadisticas/portalestudios/).",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the Superintendencia de Bancos statistics sections (superbancos.gob.ec/estadisticas/portalestudios/). Superbancos has no CKAN organization and no \"datos abiertos\" section, so this is the only path to its published statistics: monthly financial bulletins, cards/ATMs/correspondents service statistics, comparative annual bank behavior, and the publication calendar. IMPORTANT: each section page also embeds a client-side OneDrive widget that lazy-loads the *most recent* years' files outside the page HTML — not covered here. This tool only returns what is in the page's static archive tables, which for most sections stops around 2020-2021 (boletines financieros: 1997-2008 only). Follow up with get_superbancos_seccion_archivos(seccion) for one section's actual file listing and coverage."
      },
      {
        "name": "get_sipa_resumen_indicadores",
        "description": "Monthly \"Resumen de Indicadores\" PDFs from SIPA (Ministry of Agriculture), by year.",
        "params": [
          {
            "name": "anio",
            "type": "int",
            "default": null,
            "description": "Year, e.g. 2025. Earliest confirmed live: 2018."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the direct monthly PDF links for one year of SIPA's \"Resumen de Indicadores\" report, reached from the Ministry of Agriculture's \"Indicadores Sectoriales\" dashboard page. Confirmed live 2018-2026; each year is a separate page and filename conventions differ across years. Other items on the same dashboard are Tableau Server embeds or JS flipbooks with no direct file — none of those are covered by this tool."
      },
      {
        "name": "search_sipa_geoportal_capas",
        "description": "Layer catalog of the Ministry of Agriculture's geoportal.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the layer's id, name, title, or abstract, e.g. \"catastro\", \"zonificacion\", \"riesgo\", \"palmicultor\". Empty returns all layers."
          },
          {
            "name": "solo_wfs",
            "type": "bool",
            "default": "False",
            "description": "If True, only return layers with WFS (attribute data) available."
          },
          {
            "name": "categoria",
            "type": "str",
            "default": "''",
            "description": "Optional exact filter on one of the 8 top-level categories. Empty returns all."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Searches the Ministry of Agriculture's geoportal (geoportal.agricultura.gob.ec) layer catalog — a GeoServer instance exposing Ecuador's agriculture spatial layers via WMS/WFS. 277 layers confirmed live across 24 endpoints in 8 categories: administrative censuses/registries, agroecological zoning, agro-industrial infrastructure, natural-resource inventories, land cover, geomorphology, SIGTIERRAS' rural cadastre, and agroclimatic risk statistics. 257 of the 277 also expose WFS; the rest are WMS-only. Use get_sipa_geoportal_capa_datos(id) to sample a layer's actual attribute values."
      },
      {
        "name": "get_sipa_geoportal_capa_datos",
        "description": "Sample of real attribute data from a SIPA geoportal layer.",
        "params": [
          {
            "name": "layer_id",
            "type": "str",
            "default": null,
            "description": "Layer id from search_sipa_geoportal_capas, the \"categoria/store/name\" triple, e.g. \"demarcacion/E25k/vw_hg000_zae_cafe_arabigo\"."
          },
          {
            "name": "count",
            "type": "int",
            "default": "5",
            "description": "Number of features to fetch (1-20, default 5)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetches a small, bounded sample of a WFS-enabled Ministry of Agriculture geoportal layer's real feature attributes (via WFS GetFeature, JSON output). Use search_sipa_geoportal_capas first to find a layer id and check wfs_disponible=True. Not a spatial query engine: no bbox/CQL filtering, no reprojection, and geometry coordinates are dropped to keep the response small."
      }
    ]
  },
  {
    "category": "CENACE, SUT, and IG-EPN",
    "tools": [
      {
        "name": "get_cenace_tablero",
        "description": "Fetch one tablero (tab) of CENACE's live grid-operations snapshot (Ecuador's national electricity operator) — generation mix and demand, always as-of-now.",
        "params": [
          {
            "name": "tablero",
            "type": "str",
            "default": null,
            "description": "One of the 5 names above."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetch one tablero (tab) of CENACE's live grid-operations snapshot (Ecuador's national electricity operator) — generation mix and demand, always as-of-now. Tableros: - produccion_tiempo_real: current instant. - demanda_tiempo_real: current instant, plus a per-distributor (empresa eléctrica/CNEL) MW breakdown. - operativa_diaria: yesterday's full day total. - acumulada_mensual: month-to-date total (MWh). - acumulada_anual: year-to-date total (GWh). Each returns \"resumen\" (produccion total/exportación/importación/ hidráulica/térmica/renovable no convencional, or the demand equivalent). There is no historical query here — CENACE's page has no date picker, only these 5 as-of-now views."
      },
      {
        "name": "get_informe_igepn",
        "description": "Download and extract text from one IG-EPN report found via search_informes_igepn.",
        "params": [
          {
            "name": "nombre",
            "type": "str",
            "default": null,
            "description": "Exact report name from search_informes_igepn (e.g. \"Informe Diario 2022-071\")"
          },
          {
            "name": "volcan",
            "type": "str",
            "default": "''",
            "description": "Volcano name, only needed to disambiguate a duplicate nombre"
          },
          {
            "name": "grupo",
            "type": "str",
            "default": "''",
            "description": "\"sismico\" | \"volcanico\" | \"\" (both) -- same value used to find it"
          },
          {
            "name": "anio",
            "type": "int",
            "default": "0",
            "description": "Report year (0 = current year) -- same value used to find it"
          },
          {
            "name": "pages",
            "type": "str",
            "default": "''",
            "description": "Page range, 1-indexed (e.g. \"3\", \"1-5\"). Empty = whole document, capped at 20 pages/call"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Download and extract text from one IG-EPN report found via search_informes_igepn. IG-EPN has no stable per-report URL (each download is a session-bound form submit), so this re-locates the report by exact name/year/group and streams its PDF directly -- pass `volcan` to disambiguate when two reports share the same name on the same day (happens for volcanic daily reports)."
      },
      {
        "name": "get_sut_indicador_schema",
        "description": "List the queryable columns/measures/date-levels for one SUT Power BI dashboard, discovered from the report's own layout definition (every visual's underlying query), not by guessing.",
        "params": [
          {
            "name": "indicador",
            "type": "str",
            "default": null,
            "description": "A key from list_sut_indicadores."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the queryable columns/measures/date-levels for one SUT Power BI dashboard, discovered from the report's own layout definition (every visual's underlying query), not by guessing. A field like \"public contratos.nombre_padre_activ_econ_empresa\" is a plain column usable in query_sut_indicador's campos or filtros; one ending in \"[medida]\" is an aggregate measure (only valid in campos, never filtros); one ending in \"[Año]\"/\"[Mes]\" is a date-hierarchy level. denuncias_publico and encuentra_empleo use an older report layout where visuals don't expose their query this way at all — their fields come from a small manually-captured set instead (recovered by driving the live dashboard and reading the queries it actually sent), not from this automatic discovery, but they show up here the same as any other field."
      },
      {
        "name": "list_sut_indicadores",
        "description": "List the Ministerio del Trabajo/SUT's public Power BI \"Indicadores\" dashboards (sut.trabajo.gob.ec/mrl/contenido/indicadores/*.xhtml).",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List the Ministerio del Trabajo/SUT's public Power BI \"Indicadores\" dashboards (sut.trabajo.gob.ec/mrl/contenido/indicadores/*.xhtml). These are live queryable semantic models, not static files — they cover monthly contract registrations by industry/province/gender since 2015 (contratos), employer-reported labor demand and skills gaps, training/certification stats, provincial employment-gap indicators, and a gender/workplace-policy compliance dashboard (PEA, wage gaps, lactation rooms, childcare, teleworking). None of this overlaps with what's already covered by CKAN's ministerio-del-trabajo datasets, which are current-snapshot-only. Follow up with get_sut_indicador_schema(indicador) to see one dashboard's queryable fields, then query_sut_indicador to pull actual data — any combination of fields, not just what one chart already shows."
      },
      {
        "name": "query_sut_indicador",
        "description": "Run a live query against one SUT Power BI dashboard's underlying data model — any combination of its fields, e.g. month and industry together.",
        "params": [
          {
            "name": "indicador",
            "type": "str",
            "default": null,
            "description": "A key from list_sut_indicadores."
          },
          {
            "name": "campos",
            "type": "list[str]",
            "default": null,
            "description": "Field labels exactly as returned by get_sut_indicador_schema."
          },
          {
            "name": "filtros",
            "type": "dict[str, str] | None",
            "default": "None",
            "description": "Optional {campo: valor} equality filters, plain columns only."
          },
          {
            "name": "limite",
            "type": "int",
            "default": "500",
            "description": "Row cap applied server-side by the query itself."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Run a live query against one SUT Power BI dashboard's underlying data model — any combination of its fields, e.g. month AND industry together for \"contratos\" (a real monthly time series since 2015 by CIIU industry, province, gender, contract status — none of this is in the ministerio-del-trabajo CKAN datasets, which are a single current-snapshot stock count with no time dimension). Get valid campos from get_sut_indicador_schema(indicador) first — an unrecognized field name raises an error naming the tool to call instead of guessing. filtros only accepts plain-column fields (not \"[medida]\"/hierarchy-level fields) and matches exact text equality, e.g. {\"public contratos.Estado contrato\": \"Vigente\"}. Rows can run into the thousands for a wide breakdown (e.g. every month x every industry) — narrow with filtros or fewer campos rather than raising limite past what's actually needed; format \"json\" is exact but still subject to the same limite cap."
      },
      {
        "name": "search_informes_igepn",
        "description": "Search the IG-EPN PDF report archive (Instituto Geofísico): daily/weekly/monthly/special seismic bulletins, volcanic \"IG Al Instante\" alerts, field/annual reports, etc.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text over report name/volcano (e.g. \"cotopaxi\", \"trimestral\")"
          },
          {
            "name": "grupo",
            "type": "str",
            "default": "''",
            "description": "\"sismico\" | \"volcanico\" | \"\" (both)"
          },
          {
            "name": "anio",
            "type": "int",
            "default": "0",
            "description": "Report year (0 = current year)"
          },
          {
            "name": "limit",
            "type": "int",
            "default": "15",
            "description": "Max reports to return (default 15, max 30)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Search the IG-EPN PDF report archive (Instituto Geofísico): daily/weekly/monthly/special seismic bulletins, volcanic \"IG Al Instante\" alerts, field/annual reports, etc. Different from search_sismos, which reads the raw earthquake catalog feed -- this searches the narrative/PDF report archive instead. Use get_informe_igepn afterwards to download and read one report's text."
      }
    ]
  },
  {
    "category": "Research utilities",
    "tools": [
      {
        "name": "investigate_dataset",
        "description": "One-shot research shortcut: search for a dataset, list its resources, and preview the most promising one's actual data -- the search_datasets -> list_dataset_resources -> preview_resource_data workflow in a single call, for the common case of \"find me data about X and show me what's in it\"",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": null,
            "description": "Search keywords (e.g. \"empleo\", \"SRI recaudación\")"
          },
          {
            "name": "source",
            "type": "str",
            "default": "'nacional'",
            "description": "\"nacional\" (default) or \"cuenca\" (Cuenca municipal portal)"
          },
          {
            "name": "preview_rows",
            "type": "int",
            "default": "10",
            "description": "Data rows to preview from the chosen resource (default: 10, max: 50)"
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "One-shot research shortcut: search for a dataset, list its resources, and preview the most promising one's actual data -- the search_datasets -> list_dataset_resources -> preview_resource_data workflow in a single call, for the common case of \"find me data about X and show me what's in it\". Picks the top-ranked search result and, among its resources, the first one in a format this server can actually parse into a table (CSV/JSON/XLS/XLSX/ODS/ZIP/TARGZ), skipping unreadable ones (.rar, unrecognized formats) rather than previewing whichever resource happens to be listed first. Not a replacement for the individual tools: if the top search result isn't the right dataset, or you need a specific resource within it, use search_datasets/list_dataset_resources/ preview_resource_data directly instead of re-running this."
      },
      {
        "name": "list_zip_contents",
        "description": "List a .zip archive's member files (name, size) from a direct URL, without downloading the archive.",
        "params": [
          {
            "name": "url",
            "type": "str",
            "default": null,
            "description": "Direct URL to a .zip file (from another tool's result, e.g. get_inec_publicacion_archivos, search_censo_recursos)."
          },
          {
            "name": "limit",
            "type": "int",
            "default": "200",
            "description": "Max members returned (default 200, max 1000)."
          },
          {
            "name": "offset",
            "type": "int",
            "default": "0",
            "description": "Pagination offset over the member list."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "List a .zip archive's member files (name, size) from a direct URL, without downloading the archive. Reads only the End Of Central Directory record and the central directory via HTTP Range requests, so it works for archives far larger than the 5 MB preview_zip/download_resource cap -- e.g. INEC or censo microdata ZIPs of multiple hundred MB. Requires the server to honor Range requests; fails with a clear error otherwise. Does not support ZIP64 archives. Listing names is cheap this way, but previewing a specific member's rows is not (still needs decompressing from that member's offset onward) -- this tool only lists metadata."
      },
      {
        "name": "read_pdf",
        "description": "Extract text from a PDF document at a direct URL.",
        "params": [
          {
            "name": "url",
            "type": "str",
            "default": null,
            "description": "Direct URL to the PDF file"
          },
          {
            "name": "pages",
            "type": "str",
            "default": "''",
            "description": "Page range, 1-indexed (e.g. \"3\", \"1-5\", \"1,4,9\"). Empty means the whole document, still capped at 20 pages per call."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Extract text from a PDF document at a direct URL. Useful for PDFs linked from other tools' results, e.g. get_regulacion_info's Registro Oficial file, or a dataset resource that turned out to be a PDF bulletin instead of a tabular file. No OCR: a scanned PDF with no embedded text layer will come back empty. Max download size: 5 MB (larger files return an explicit error instead of a partial read -- PDFs can't be parsed from a truncated download); max 20 pages extracted per call (call again with a different `pages` range for the rest)."
      }
    ]
  },
  {
    "category": "SEPS and the popular and solidarity economy",
    "tools": [
      {
        "name": "list_seps_secciones",
        "description": "SEPS statistics sections (popular and solidarity financial sector).",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the statistics sections published by the Superintendencia de Economía Popular y Solidaria (estadisticas.seps.gob.ec). SEPS' main site blocks automated connections, but this statistics subdomain has no CKAN organization: 22 sections on the popular/solidarity financial sector plus 4 on non-financial organizations. Includes yearly risk-rating bulletins, 2020-2025. Follow up with get_seps_seccion_archivos(seccion)."
      },
      {
        "name": "get_seps_seccion_archivos",
        "description": "Documents in one SEPS statistics section.",
        "params": [
          {
            "name": "seccion",
            "type": "str",
            "default": null,
            "description": "A section key from list_seps_secciones, e.g. \"sfps_reportes_calificacion_de_riesgos\"."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the direct download links published on one SEPS statistics section page. Returns the section's own description plus, per file, the grupo, the periodo/título, direct URL, and format. Some links are static; others route through the site's download-monitor plugin and format comes back unknown for those."
      }
    ]
  },
  {
    "category": "IESS: institutional documents",
    "tools": [
      {
        "name": "list_iess_colecciones",
        "description": "IESS document collections (bulletins, actuarial studies, audits).",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists IESS's (Ecuadorian Social Security Institute) three document-archive collections — Boletines Estadísticos, Estudios Actuariales, and Informes de Auditoría — with the years/counts each currently has published. Call get_iess_archivos(coleccion, anio=...) next to get one collection's actual documents."
      },
      {
        "name": "get_iess_archivos",
        "description": "Documents in one IESS collection.",
        "params": [
          {
            "name": "coleccion",
            "type": "Literal['boletines', 'estudios_actuariales', 'informes_auditoria']",
            "default": null,
            "description": "\"boletines\" (annual, 1978-2024 confirmed), \"estudios_actuariales\" (per fund, only a handful of years), or \"informes_auditoria\" (2007-2026 confirmed, by year)."
          },
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Filter to one year. Required for \"informes_auditoria\". Optional for the other two collections."
          },
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against título and, where available, descripción/grupo."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists one IESS document collection's actual documents, each resolved to a direct download URL, título, and formato. Call list_iess_colecciones first to see what years/counts each collection has."
      }
    ]
  },
  {
    "category": "INEVAL: educational evaluation",
    "tools": [
      {
        "name": "list_ineval_familias",
        "description": "INEVAL evaluation families with a database download page.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists INEVAL (National Institute for Educational Evaluation) evaluation families with a real \"Bases de Datos\" download page — a different institution from SENESCYT/MINEDUC. Nine families are published: Ser Bachiller, Ser Estudiante (with variants), Ser Maestro, Ser Profesional, and Llece (the international ERCE/SERCE/TERCE rounds). Follow up with get_ineval_familia_archivos(familia)."
      },
      {
        "name": "get_ineval_familia_archivos",
        "description": "Download links for one INEVAL evaluation family.",
        "params": [
          {
            "name": "familia",
            "type": "str",
            "default": null,
            "description": "A family key from list_ineval_familias, e.g. \"ser_bachiller\", \"ser_estudiante\", \"llece\"."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the direct download links published on one INEVAL evaluation family's \"Bases de Datos\" page. Returns each file's grupo, periodo, título, and formato (CSV, SAV, XLSX, SINTAXIS, METADATO, DICCIONARIO, or unknown). Some files are several MB — download the URL directly instead of routing it through preview_resource_data or download_resource, which cap at 5 MB."
      }
    ]
  },
  {
    "category": "Civil aviation (DGAC)",
    "tools": [
      {
        "name": "get_metar",
        "description": "Recent METAR/SPECI weather reports for an Ecuadorian aerodrome.",
        "params": [
          {
            "name": "designador",
            "type": "str",
            "default": null,
            "description": "ICAO code of the aerodrome/helipad, e.g. SEQM (Quito), SEGU (Guayaquil), SECU (Cuenca)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetches the most recent METAR/SPECI aerodrome weather reports for an Ecuadorian aerodrome or helipad, from DGAC's IFIS site (ais.aviacioncivil.gob.ec) — publicly queryable with no login. Returns each report's type, its UTC observation timestamp, and the raw ICAO-format codification text. An unknown designador returns an empty result rather than an error."
      },
      {
        "name": "get_notam",
        "description": "Active NOTAMs for an Ecuadorian aerodrome.",
        "params": [
          {
            "name": "designador",
            "type": "str",
            "default": null,
            "description": "ICAO code of the aerodrome/helipad, e.g. SEQM (Quito), SEGU (Guayaquil), SECU (Cuenca)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetches active NOTAMs (Notices to Airmen) for an Ecuadorian aerodrome or helipad, from DGAC's IFIS site, also publicly queryable with no login. Each NOTAM includes its series number, the raw ICAO-format text, and the site's own decoded field table. An unknown designador returns zero NOTAMs rather than an error."
      },
      {
        "name": "get_sigmet",
        "description": "Active SIGMETs for Ecuador's single FIR.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetches currently active SIGMETs (significant meteorological information — volcanic ash, severe turbulence/icing, thunderstorms) for Ecuador's FIR, from DGAC's IFIS site. Ecuador has a single FIR (SEFG), so there is no per-aerodrome parameter. Zero results means no advisories are currently active, not an error."
      },
      {
        "name": "list_aip_aerodromos",
        "description": "Aerodromes/helipads with a published AIP data sheet.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the ICAO designators and names of every aerodrome/helipad with a published AIP AD 2.x page, from DGAC's public eAIP — no login required. Use get_aip_aerodromo with one of these designators to fetch its full data sheet (coordinates, runway, operating hours, fees, ATS frequencies, etc.)."
      },
      {
        "name": "get_aip_aerodromo",
        "description": "Full AIP AD 2.x data sheet for an Ecuadorian aerodrome.",
        "params": [
          {
            "name": "designador",
            "type": "str",
            "default": null,
            "description": "ICAO code of the aerodrome/helipad, e.g. SEQM (Quito), SEGU (Guayaquil), SECU (Cuenca)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Fetches the published AIP AD 2.x data sheet for an Ecuadorian aerodrome or helipad, from DGAC's public eAIP — no login required. Returns every numbered field the AIP publishes, grouped by ICAO Annex 15 subsection: location and administrative data, operating hours, rescue-and-firefighting services, apron/taxiway data, radio navigation aids, runway physical characteristics, declared distances, lighting, flight procedures, and supplementary information. Use list_aip_aerodromos to see which designators are published."
      }
    ]
  },
  {
    "category": "ARCOTEL: telecommunications",
    "tools": [
      {
        "name": "search_arcotel_reportes_mensuales",
        "description": "ARCOTEL's monthly telecom statistical reports.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the entry's label, year, or URL (accent-insensitive), e.g. \"june 2026\", \"2025\", \"internet\". Empty returns all entries."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists ARCOTEL's (telecom regulator) \"Reportes Estadísticos Mensuales\" PDFs, published on the institutional site outside CKAN. Confirmed live range: January 2017 through June 2026, typically lagging today by about two months. A separate, higher-frequency series from search_arcotel_boletines and from ARCOTEL's CKAN organization (frozen since 2021/2022). PDF only — returns direct URLs, not parsed table data."
      },
      {
        "name": "search_arcotel_boletines",
        "description": "ARCOTEL's annual/topical telecom statistical bulletins.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the entry's label, year, or URL (accent-insensitive), e.g. \"roaming\", \"2020\", \"portabilidad\". Empty returns all entries."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists ARCOTEL's \"Boletín Estadístico del Sector de las Telecomunicaciones\" PDFs, published on the institutional site outside CKAN. Confirmed live range: 2015 through 2024 (no 2025/2026 bulletin yet). Lower-frequency and more topic-driven than search_arcotel_reportes_mensuales. PDF only — returns direct URLs, not parsed table data."
      }
    ]
  },
  {
    "category": "ARCSA: sanitary registry",
    "tools": [
      {
        "name": "list_arcsa_categorias",
        "description": "Categories of ARCSA's live sanitary registry.",
        "params": [
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists ARCSA's \"Base de Registros Emitidos\" categories (controlsanitario.gob.ec/base-de-datos/) — the live sanitary registry for food, medications, cosmetics, medical devices, natural products, pesticides, and related establishments/permits: 27 top-level categories with 77 documents total. A different source from ARCSA's CKAN datasets, which only cover suspended or cancelled records. Some categories nest further sub-categories (e.g. by year), shown as each document's subgrupo in get_arcsa_categoria_archivos."
      },
      {
        "name": "get_arcsa_categoria_archivos",
        "description": "Documents in one ARCSA sanitary-registry category.",
        "params": [
          {
            "name": "categoria",
            "type": "str",
            "default": null,
            "description": "A category \"id\" or \"nombre\" from list_arcsa_categorias (nombre match is accent/case-insensitive)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists one ARCSA \"Base de Registros Emitidos\" category's documents. Returns each document's subgrupo (nested sub-category, when present), título, id, and direct URL. Format is reported as unknown because the download link carries no file extension, though a spot check confirmed real files behind it."
      }
    ]
  },
  {
    "category": "Additional sector sources",
    "tools": [
      {
        "name": "search_cnig_femicidios",
        "description": "CNIG gender-violence statistics, including femicides.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched against the entry's label (accent-insensitive), e.g. \"femicidio\", \"lgbti\", \"provincia\". Empty returns all 20 entries on the page."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists the statistical PDFs on the National Council for Gender Equality's (CNIG) \"Violencia\" page — the femicide / intentional-homicide-of-women matrix plus 19 related gender-violence tables. The PDF states its figures are compiled from the Judiciary Council, the Attorney General's Office, and the Ministry of the Interior, and describes the indicator as weekly-updated — though what's posted is a dated snapshot (cut off April 2023), not a live feed."
      },
      {
        "name": "search_minedec_matricula",
        "description": "Historical enrollment registry for K-12 education.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the file's label, tipo (\"registro\"/\"metadato\"/\"diccionario\"), periodo (\"inicio\"/\"fin\"), or URL. Empty returns all files."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists MINEDEC's (Ministry of Education, Sport and Culture) historical basic-education (K-12) enrollment registry files, 2009-present. A different ministry sub-domain from this project's existing SENESCYT/higher-education CKAN coverage. Returns direct XLSX links, not file contents — the registry files are 30-140 MB, too large to preview or download through this server."
      },
      {
        "name": "search_mef_fiscal",
        "description": "Ecuador's fiscal-operations figures, including tariff revenue.",
        "params": [
          {
            "name": "fuente",
            "type": "str",
            "default": "'mef'",
            "description": "\"mef\" (default) or \"senae\"."
          },
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the file's label (and, for \"senae\", its category), e.g. \"ingresos\", \"2026-06\", \"advalorem\", \"totales\". Empty returns all files for the chosen fuente."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists Ecuador's fiscal-operations workbook links from either of two sources. fuente=\"mef\" (default): the Ministry of Finance/MDEP's \"Estadística Nueva Metodología\" archive — IMF GFSM-methodology fiscal accounts, updated monthly, 76 XLSX files confirmed live. For tariff income, download the newest \"Operaciones de Ingresos y Gastos SPNF\" file and read the \"1214 Arancelarios\" row. fuente=\"senae\": SENAE's \"Tributos Recaudados\" archive, 60 XLSX confirmed live but stale (2012-2021 only). \"Arancelarios\"/\"ADVALOREM\" are the tariff duty alone — narrower than the broader customs-collection figure some press coverage cites, which also includes VAT and excise tax."
      },
      {
        "name": "search_infomies_bases_mensuales",
        "description": "Monthly infoMIES databases (economic and social inclusion).",
        "params": [
          {
            "name": "serie",
            "type": "str",
            "default": null,
            "description": "\"anc\" (non-contributory insurance / economic inclusion) or \"is\" (SIIMIES users / social inclusion)."
          },
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Specific year to fetch, e.g. 2024. Omit to fetch every known year and aggregate (multiple cold-cache requests)."
          },
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the file's label, year, or URL, e.g. \"julio\", \"2023\". Empty returns all files for the selected year(s)."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists infoMIES's (statistics portal of the former MIES) monthly database files for one program: \"anc\" or \"is\". Richer than this project's existing CKAN coverage for the same programs, which only has quarterly snapshots. Confirmed live: only the current year has one file per month so far; every closed year (2019-2025) has exactly one file, the December/year-end snapshot. Files are .rar (outside this project's supported-format list) — only metadata and the direct URL are returned."
      },
      {
        "name": "search_infomies_boletines_zonales",
        "description": "infoMIES zonal bulletins, by zone or consolidated by year.",
        "params": [
          {
            "name": "modo",
            "type": "str",
            "default": "'zonal'",
            "description": "\"zonal\" or \"consolidado\"."
          },
          {
            "name": "zona",
            "type": "str",
            "default": "''",
            "description": "Required for modo=\"zonal\": one of \"zona-1-bz\"..\"zona-9-bz\". Ignored for modo=\"consolidado\". Leave empty with modo=\"zonal\" to get the list of valid zone keys."
          },
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Specific year to fetch. For \"zonal\", omit to fetch every year found (typically 2017-2021). For \"consolidado\", omit to fetch 2021-2026."
          },
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the file's label, year, or URL."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists infoMIES's zonal bulletin files. Two distinct series live under this name: modo=\"zonal\" (default), the original \"Boletín Zonal\" series (one .rar per zone per month), discontinued (2017-2021, nothing since); modo=\"consolidado\", the newer \"Reporte Boletines Zonales\" successor (one consolidated XLSX per year, 2021-2026, still being updated). Both return metadata and direct URLs only — the .rar files are additionally outside this project's supported-format list."
      },
      {
        "name": "search_trabajo_boletin_anual",
        "description": "Annual statistical bulletin on Ecuador's labor market.",
        "params": [
          {
            "name": "query",
            "type": "str",
            "default": "''",
            "description": "Free text matched (accent-insensitive) against the edition's year, title, or URL, e.g. \"2021\". Empty returns all known editions."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists known editions of the Ministry of Labor's annual statistical bulletin on Ecuador's labor market — a PDF report sourced from INEC's ENEMDU survey plus the ministry's own administrative registries. Not a complete historical series — only 3 editions are known (2020, 2021, 2022). The page that once listed multiple editions cannot be scraped live today and now only links the 2022 edition; 2020 and 2021 were recovered from a January 2024 Wayback Machine snapshot."
      },
      {
        "name": "search_salarios_sectoriales",
        "description": "Ecuador's sectoral minimum wage tables.",
        "params": [
          {
            "name": "anio",
            "type": "int | None",
            "default": "None",
            "description": "Filter to this year (as printed in the document's own title). None returns every year found."
          },
          {
            "name": "format",
            "type": "str",
            "default": "'text'",
            "description": "text | json"
          }
        ],
        "long_description": "Lists Ecuador's sectoral minimum wage table documents — wage floors per branch of economic activity, set by the Consejo Nacional de Trabajo y Salarios, distinct from the single national Salario Básico Unificado. Scraped from the Ministry of Labor's document library, which actually responds and lists stable per-document download links unlike the ministry's dynamic pages. Confirmed coverage: one entry per year for 2020-2025. No 2026 table has been published — the 2025 table remains in force by default."
      }
    ]
  }
]
